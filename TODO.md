# TODO — продуктовое, UI/UX и техническое ревью

Дата: 2026-09-25.

## Область и достоверность ревью

Проверены исходники frontend, Express API, Google Apps Script, SQL-миграции, сборка, существующие тесты и npm audit основных двух пакетов. Ниже — обнаруженные проблемы и конкретные задачи, а не утверждение, что найдены абсолютно все дефекты приложения.

- **Код** — причина подтверждена чтением реализации; сценарий в production не запускался.
- **Проверка** — дополнительно есть результат локальной команды или синтетического воспроизведения формулы/условия. Синтетические проверки не являются полноценными интеграционными тестами приложения.
- Фактические RLS/grants, настройки GAS, reverse proxy, планировщика Windows и доступность внешних сервисов не проверялись. Выводы о них основаны на конфигурации репозитория, а не на обследовании production.
- UI проверен по компонентам, состояниям и обработчикам. Визуального прогона в браузере, проверки контраста, screen reader, камеры, микрофона и реальных iOS/Android-устройств не было.
- Файлы с секретами и содержимое сессии портала не открывались. Запись/удаление данных во внешних системах не выполнялись. Зависимости не обновлялись, миграции не применялись.
- Исходники приложения не изменялись. Проверка сборки обновила игнорируемый `dist/`. Существовавшее изменение `ai-consultant-api/downloads/.session.json` оставлено нетронутым.

## Приоритеты

- **P0** — проверить и закрыть до следующего публичного релиза: отсутствие границ доступа или потенциальная утечка сессии.
- **P1** — высокий: неверные бизнес-данные, потеря/дублирование записей, безопасность, сломанный основной сценарий.
- **P2** — средний: UX, надёжность, сопровождение, неполная реализация заявленных возможностей.
- **P3** — низкий: локальные несоответствия и уборка технического долга.

Приоритет не означает, что эксплуатация уязвимости уже подтверждена в production.

## Выполненные проверки

| Команда / проверка | Результат |
| --- | --- |
| `npm run build` в корне | Успешно: TypeScript + Vite production build. Есть предупреждения о смешанных static/dynamic imports `supabaseAuthApi.ts` и `sessionStorage.ts`. |
| `npm run test:run` в корне | 27 тестов, 4 файла — успешно. |
| `npm test` в `ai-consultant-api` | 77 тестов, 9 файлов — успешно. |
| Локальный `tsc --noEmit` в `ai-consultant-api` | Успешно. |
| `npm audit --omit=dev` в корне | 7 уязвимых зависимостей: 5 high, 2 moderate. Exit code 1 — ожидаемый результат audit, не ошибка запуска. |
| `npm audit --omit=dev` в `ai-consultant-api` | 8: 5 high, 2 moderate, 1 low. Наборы двух пакетов пересекаются; это не 15 уникальных уязвимостей. |
| `npm ls @vitest/coverage-v8 --depth=0` | Пакет отсутствует. |
| Синтетические проверки логики | Подтверждены двойной учёт после пропуска дня, превышение 5 МиБ после Base64 для фото 4 МиБ, срабатывание upload-shortcut на отрицание, `Infinity` при предыдущем расходе 0. |

Зелёные тесты не покрывают перечисленные ниже сценарии автоматически. Для закрытия задач нужны регрессионные тесты и проверки критериев готовности.

## 1. Безопасность и границы доступа

### SEC-01
- [x] **P0 — Защитить маршруты журнала ТО и файлов в Express.**
  - **Сделано 2026-09-25:** `router.use(authMiddleware)` на `/api/equipment/*`; frontend `maintenanceApi` шлёт Bearer; тесты `equipment.auth.test.ts` (anonymous/invalid → 401 до GAS; valid → чтение/запись).
  - **Остаток в SEC-05:** проверка права на конкретный раздел/оборудование (не только «вошёл»).

### SEC-02
- [x] **P0 — Закрыть прямой обход авторизации через Google Apps Script.**
  - **Сделано 2026-09-25 (частично, рабочий минимум):** add/update/delete/create-folder оборудования идут через Express (`auth` + `admin`); frontend без no-cors/GAS для этих мутаций; GET `addMaintenanceEntry` в GAS отключён; опциональный `GAS_API_SECRET` / Script Property `API_SECRET` для POST-мутаций; тесты SEC-02 в `equipment.auth.test.ts`.
  - **Остаток:** задеплоить обновлённый `Code.gs` + задать `API_SECRET` в Script Properties и `GAS_API_SECRET` на Railway; остальные прямые GAS-мутации (доступ/Beliot/workshop) — следующими итерациями.

### SEC-03
- [x] **P0 — Убрать runtime-файл cookies портала из отслеживания Git и оценить возможную компрометацию.**
  - **Сделано 2026-09-25:** `git rm --cached` для `ai-consultant-api/downloads/.session.json`; файл на диске сохранён; `.gitignore` усилен. История Git не переписывалась.
  - **На оценку владельцу:** при необходимости сменить пароль/сессию bvod.by, если cookies когда-либо попали в удалённый репозиторий.

### SEC-04
- [x] **P0 — Ограничить привилегированные SQL RPC и не рассчитывать на RLS внутри SECURITY DEFINER.**
  - **Сделано 2026-09-25:** миграция `supabase/migrations/20260925_harden_security_definer_rpcs.sql` применена в Supabase — auth-checks в `log_login` / `get_login_history_with_email`; `insert_beliot_reading` EXECUTE только `service_role`; REVOKE PUBLIC. Frontend `getLoginHistory` передаёт свой `p_user_id` не-админу. Unit-тесты в `supabaseAuthApi.test.ts`; verify SQL: `supabase/tests/sec04_rpc_grants.verify.sql`.
  - **Остаток:** опционально прогнать verify SQL под JWT anon/user/admin в SQL Editor.

### SEC-05
- [x] **P1 — Сделать доступ к разделам сквозным: маршруты, RLS, API и AI-инструменты.**
  - **Сделано 2026-09-25 (slice):** UI — `AppAccessGuard` на `/equipment/:id`, forms и `/water-quality/analysis/*`. SQL — `has_app_access` + RLS water-таблиц/показаний (`20260925_sec05_app_access_rls.sql`, применена в Supabase). AI — `loadUserAppAccess` + `filterToolsByAccess` в chat/chatStream; `executeToolCall` повторно проверяет доступ через `toolContext.appAccess`. Тесты: `toolAccessPolicy.test.ts`; verify: `supabase/tests/sec05_app_access.verify.sql`.
  - **Остаток:** детальный ACL uploads в конкретную папку Drive (вне этого slice); water Express routes без отдельного app-guard (полагаются на RLS/auth).

### SEC-06
- [x] **P1 — Разделить общую и персональную память агента и ограничить её изменение.**
  - **Сделано 2026-09-25:** миграция `20260925_sec06_agent_memory_scopes.sql` применена в Supabase (`scope`, `user_id`, `created_by`; shared/personal unique indexes; RLS read shared+own). Сервис `saveSharedFact` / `savePersonalFact` / `loadFactsForUser`; промпт грузит shared+personal по `userId`. Tools: `preference` → personal; shared write/delete → admin; тесты `memoryTools.test.ts`, `agentMemoryService.test.ts`.
  - **Остаток:** нет (slice закрыт).

### SEC-07
- [x] **P1 — Удалить глобальное отключение проверки TLS в сборщике.**
  - **Сделано 2026-09-25:** убран `NODE_TLS_REJECT_UNAUTHORIZED=0` из `collect-beliot-readings.ts`; добавлен `ensureTlsVerificationEnabled` (`scripts/tlsPolicy.ts`); в workflow `NODE_ENV=production`. Проверено: `beliot.by:4443` проходит строгий TLS; тесты `scripts/__tests__/tlsPolicy.test.ts` (сброс insecure env, reject expired.badssl.com, accept Beliot).
  - **Остаток:** если Beliot внезапно сменит cert на недоверенный — по команде добавить узкий аварийный флаг только для Beliot.

### SEC-08
- [x] **P1 — Исключить тела чувствительных запросов из логов GAS.**
  - **Сделано 2026-09-25:** `sanitizeForLog` / `safeJsonForLog` / `summarizePostEventForLog` в `Utils.gs`; `doPost` больше не пишет сырой body/параметры с паролями. Эталон + тесты: `scripts/gasLogSanitize.ts`, `scripts/__tests__/gasLogSanitize.test.ts`.
  - **Остаток:** задеплоить обновлённые `Code.gs` + `Utils.gs` в Apps Script.

### SEC-09
- [x] **P1 — Разобрать и устранить подтверждённые npm audit предупреждения production-зависимостей.**
  - **Сделано 2026-09-25:** корень и `ai-consultant-api` — `npm audit --omit=dev` = **0**. Обновлены transitive/direct fix'ы (`react-router-dom` → 7.18.4 и др.). `qs` через `overrides` ≥6.16.0. Уязвимый `xlsx` заменён на `exceljs` в `browserService.ts` (+ тест `excelWorkbookParse.test.ts`). Старый `.xls` больше не парсится (сообщение сохранить как `.xlsx`).
  - **Остаток:** без `--force`; dev-зависимости могут ещё показывать предупреждения — вне scope production.

## 2. Бизнес-логика и целостность данных

### DATA-01
- [x] **P1 — Исправить двойной учёт расхода после пропущенных суток.**
  - **Сделано 2026-09-25:** `computeDayConsumption` берёт последнее известное показание, а не baseline начала месяца. Пропуск помечается `unknownDistribution` и текстом в панели дня графика. День замены по-прежнему `max − min` и не вбирает расход старой шкалы. Тесты: `dayConsumption.test.ts` (один пропуск, несколько, 1-е число, пропуск с начала месяца, замена).
  - **Остаток:** нет.

### DATA-02
- [x] **P1 — Унифицировать расчёты после замены счётчика и применение ручных корректировок.**
  - **Сделано 2026-09-26:** архив, график, KPI и `analyze_water_consumption` считают `consumptionFromDailySeries`: сумма дней, корректировка дня вместо `max − min`, разрыв шкалы в день замены. Месячный KPI ХВО больше не `max − min` внутри месяца. Тесты: обычный месяц, замена в середине и на 1-е число, корректировка 3.48 (`dayConsumption.test.ts`, `useDeviceArchive.volume.test.ts`, `meterConsumption.test.ts`).
  - **Остаток:** нет.

### DATA-03
- [x] **P1 — Убрать зависимость полноты аналитики от лимита выдачи Supabase и глобального LIMIT для baseline.**
  - **Сделано 2026-09-26:** `fetchAllPages` дочитывает показания дашборда и `water_invoices`, пока страница короче лимита. Baseline — `limit(1)` отдельно на каждый `device_id`. Тесты: страница не обрывает набор; общий лимит новейших строк теряет редкий счётчик, отдельный запрос его сохраняет (`pagedSelect.test.ts` во frontend и API).
  - **Остаток:** нет.

### DATA-04
- [x] **P1 — Отличать отсутствие данных от нулевого потребления и ошибок загрузки.**
  - **Сделано 2026-09-26:** карточки пишут «нет измерений», если по роли нет показаний, и «0 м³», если расход измерен и равен нулю. Неполный ряд подписан отдельно. Отрицательный баланс скважина − производство не зажимается в 0. Ошибка загрузки оставляет прежние цифры, показывает причину, время обновления и «Повторить». Тесты: `balanceStatus.test.ts`.
  - **Остаток:** нет.

### DATA-05
- [x] **P1 — Защитить загрузки от гонок при смене оборудования и периода.**
  - **Сделано 2026-09-26:** перед записью сверяется поколение запроса. Поздний ответ и ошибка предыдущего оборудования или месяца не меняют экран. Тесты: `useEquipmentData.requestRace.test.tsx`, `WaterDashboard.requestRace.test.tsx`.
  - **Остаток:** нет.

### DATA-06
- [x] **P1 — Сохранять анализ и результаты как одну логическую операцию.**
  - **Сделано 2026-09-26:** форма вызывает `save_water_analysis_bundle`. Повтор с тем же id обновляет анализ. Сбой второго результата или удаления очищенного значения откатывает всю запись и не показывает успех. Тесты: `analysisSavePlan.test.ts`. Миграция применена в Supabase.
  - **Остаток:** нет.

### DATA-07
- [x] **P1 — Не переписывать историческое авторство анализа текущим редактором.**
  - **Сделано 2026-09-26:** правка без галочки не передаёт sampled/analyzed/responsible. Редактор пишется в `updated_by`. Смена ответственного — отдельное действие. Тест: `analysisAuthorship.test.ts`. Миграция применена в Supabase.
  - **Остаток:** нет.

### DATA-08
- [x] **P2 — Сбрасывать связь оборудования при смене точки отбора без equipmentId.**
  - **Сделано 2026-09-26:** выбор точки всегда подставляет её оборудование или очищает поле. Тест: точка A с оборудованием, точка B без него (`samplingPointEquipment.test.ts`).
  - **Остаток:** нет.

### DATA-09
- [x] **P1 — Развести статус выполнения анализа и заключение о соответствии нормам.**
  - **Сделано 2026-09-26:** статус — «в работе / завершён / отменён». Заключение считается отдельно: без измерений или без проверки норм это «не проверено», превышение не называется нормой. Ручная «норма» требует основание. Тесты: `complianceConclusion.test.ts`. Миграция применена в Supabase.
  - **Остаток:** нет.

### DATA-10
- [x] **P2 — Сделать жизненный цикл PDF-вложений согласованным с записью анализа.**
  - **Сделано 2026-09-26:** PDF принадлежит записи анализа. Ссылка снимается раньше файла: сбой базы оставляет файл доступным. Сбой записи новой ссылки удаляет только что загруженный объект. Удаление анализа сначала снимает запись, затем чистит файлы и сообщает, если файл остался. Тесты: `analysisAttachmentLifecycle.test.ts`.
  - **Остаток:** нет.

### DATA-11
- [x] **P1 — Использовать идентификатор счёта вместе с лицевым счётом во всём пути уведомления/скачивания.**
  - **Сделано 2026-09-26:** сохранённый счёт отдаёт свой id в уведомление. Скачивание идёт по id или по паре период + лицевой счёт. Запрос только по периоду отклоняется. Тесты: `invoiceIdentity.test.ts`, `invoiceDownloadQuery.test.ts`.
  - **Остаток:** нет.

### DATA-12
- [x] **P1 — Сравнивать потребление по одному лицевому счёту и корректно обрабатывать нулевую базу.**
  - **Сделано 2026-09-26:** рост считается с предыдущим периодом того же лицевого счёта, пропущенный месяц не подменяется чужим. Нулевой предыдущий объём не становится процентом. Тарифы пишутся ключом `tariff_water:{счёт}` и `tariff_sewage:{счёт}`. Тесты: `invoiceComparison.test.ts`.
  - **Остаток:** нет.

### DATA-13
- [x] **P1 — Восстанавливать PDF после частично успешного импорта счёта.**
  - **Сделано 2026-09-26:** запись без `storage_path` не пропускается следующим incremental sync. Неудачная повторная загрузка оставляет прежний путь. Тест: `invoiceFileState.test.ts`.
  - **Остаток:** нет.

### DATA-14
- [x] **P1 — Не повторять неидемпотентные операции GAS как обычные GET-запросы.**
  - **Сделано 2026-09-26:** POST в GAS уходит один раз и несёт `operationId`. Если ответ потерян, клиент читает уже записанный результат, а не создаёт объект заново. Повтор того же id на стороне Apps Script возвращает первый результат. Тест: `gasMutation.test.ts`.
  - **Остаток:** задеплоить `Code.gs`, `ResponseHelpers.gs` и `OperationIdempotency.gs` в Apps Script вручную. Клиентский no-cors fallback уже отключён.

## 3. AI-консультант

### AI-01
- [x] **P1 — Согласовать лимиты фото, истории и HTTP payload.**
  - **Сделано 2026-09-26:** тело чата принимается до 16 МБ, этого хватает на одно фото 10 МБ после Base64. В следующий запрос уходят байты только последнего фото, старые остаются на экране. Файл больше 10 МБ, неверный тип или пачка, которая не помещается, показываются ошибкой до отправки. Тесты: `chatPhotoBudget.test.ts`, `chatHistoryPayload.test.ts`, `jsonBodyLimit.test.ts`.
  - **Остаток:** нет.

### AI-02
- [x] **P1 — Заменить распознавание намерения загрузить фото по подстроке явным действием.**
  - **Сделано 2026-09-26:** фото пишутся на Диск только если пользователь включил «Записать эти фото в папку оборудования» и папка известна. Текст со словом «загрузка» сам запись не запускает. Уже записанный файл повторно не отправляется. Тесты: `photoUploadIntent.test.ts`.
  - **Остаток:** нет.

### AI-03
- [ ] **P2 — Обновлять waterContext в callbacks отправки и retry.**
  - **Код:** `src/features/ai-consultant/hooks/useChat.ts:331-335,401,435-439,468`.
  - `waterContext` используется внутри `useCallback`, но отсутствует в dependencies обеих функций. После изменения месяца/показателей без изменения остальных зависимостей в AI может уйти предыдущий контекст.
  - **Готово, когда:** сообщение и повтор получают явно определённый актуальный/зафиксированный контекст; тест меняет waterContext без новых сообщений и проверяет тело следующего запроса. Подключить правило exhaustive-deps.

### AI-04
- [ ] **P2 — Реализовать обещанную фоновую память диалога в используемом streaming-пути.**
  - **Код:** `ai-consultant-api/src/routes/ai/chat.ts:76-80,132-136`; `ai-consultant-api/src/routes/ai/chatStream.ts:67-106`; `src/features/ai-consultant/hooks/useChat.ts:189,331`.
  - Обычный `/api/chat` загружает recent history, используемый интерфейсом `/stream` — нет. UI начинает с пустого массива, endpoint `/history` всегда возвращает пустой массив. Поэтому сохранение сообщений в БД само по себе не обеспечивает заявленное продолжение прошлого разговора. Долговременные facts не заменяют историю диалога.
  - **Готово, когда:** определён продуктовый контракт «новый разговор/продолжить/забыть»; streaming использует нужную историю без дублей и смешения контекстов. Тест перезагрузки и вопроса, зависящего от предыдущей реплики.

### AI-05
- [ ] **P1 — Валидировать streaming-протокол и не считать оборванный ответ успешным.**
  - **Код:** `ai-consultant-api/src/routes/ai/chatStream.ts:39-49,68`; `src/features/ai-consultant/services/consultantApi.ts:167-190`; `src/features/ai-consultant/hooks/useChat.ts:331-355`.
  - SSE endpoint проверяет только массив/количество сообщений, в отличие от обычного chat. Элементы, roles, content blocks, длины и context не валидируются. Клиент выходит по EOF без обязательного `done`, молча пропускает невалидный JSON, не обрабатывает остаток буфера. Чистое преждевременное закрытие потока выглядит как успешный неполный ответ.
  - **Готово, когда:** оба endpoint используют один runtime-validator; клиент различает done/error/abort/unexpected EOF, корректно освобождает reader; тесты chunk boundaries, повреждённого события, EOF без done и неправильных входных данных.

### AI-06
- [ ] **P1 — Протянуть отмену запроса до AI-провайдера и побочных действий.**
  - **Код:** `src/features/ai-consultant/hooks/useChat.ts:205-210,473-478`; `ai-consultant-api/src/routes/ai/chatStream.ts:57-97`; `ai-consultant-api/src/services/ai/providers/ClaudeProvider.ts` и `DeepSeekProvider.ts` (`streamChat`).
  - Frontend abort закрывает запрос, но сервер не связывает disconnect с AbortSignal и agent loop. `writeEvent` проверяет только `writableEnded`; прекращение чтения клиентом не отменяет оплачиваемую генерацию и инструменты записи.
  - **Готово, когда:** disconnect/отмена останавливает дальнейшие LLM-вызовы и новые мутации, освобождает ресурсы; UI объясняет, что уже выполненную запись отменить закрытием чата нельзя. Добавить серверную проверку подтверждения значимых записей, а не полагаться только на инструкцию в description tool.

### AI-07
- [ ] **P2 — Привести fallback провайдеров к реальному контракту доступности.**
  - **Код:** `ai-consultant-api/src/services/ai/ProviderFactory.ts:80-125`; `providers/ClaudeProvider.ts:290-291`, `providers/DeepSeekProvider.ts:179-180`, `providers/GeminiProvider.ts:297-305` в том же каталоге.
  - Claude/DeepSeek считаются доступными по наличию клиента/ключа: при реальных 429/5xx фабрика уже отработала, запасной провайдер не вызывается. Gemini, наоборот, делает отдельный `generateContent('test')` при каждом создании, расходуя квоту и увеличивая задержку.
  - **Готово, когда:** fallback работает на определённые ошибки без повторения уже выполненных tools, проверки доступности не тратят запрос на каждую реплику; документированы ограничения после начала streaming. Тесты 429/5xx, невалидного ключа и частично выполненной мутации.

### AI-08
- [ ] **P2 — Изолировать и инвалидировать кэш AI-инструментов.**
  - **Код:** `ai-consultant-api/src/tools/index.ts:217-230,275-295`.
  - Read tools кэшируются по имени и input без user/access scope, хотя контекст запроса существует отдельно. После `add_maintenance_entry`, `save_memory` и других записей нет сброса соответствующих чтений: последующий запрос до 5 минут может показывать старое состояние. Истёкшие записи Map не удаляются по времени/лимиту.
  - **Готово, когда:** права проверяются до чтения кэша, ключ учитывает нужную область доступа, write tools инвалидируют связанные записи, есть ограничение размера/очистка. Тесты read → write → read и два пользователя с разными правами/контекстами.

## 4. Продуктовые сценарии и UI/UX

### UX-01
- [ ] **P1 — Не показывать демонстрационные характеристики и чужой QR при ошибке/отсутствии данных.**
  - **Код:** `src/features/equipment/pages/EquipmentPage.tsx:209-229`; `src/features/equipment/components/EquipmentPlate.tsx:28-33`; `src/features/equipment/services/equipmentQueries.ts:68-70`.
  - При несуществующем ID или ошибке API карточка всё равно рисует `filterSpecs`, а при отсутствующем `qrCodeUrl` генерирует QR на жёстко заданную папку Drive. Сетевая ошибка дополнительно превращается в «оборудование не найдено».
  - **Готово, когда:** отдельные состояния not-found/error/retry, никакой демонстрационной карточки в production, QR строится только для проверенного объекта или отсутствует с объяснением. Проверить неверный ID, offline и запись без ссылки.

### UX-02
- [ ] **P2 — Не выдавать сбой загрузки доступов за отсутствие прав.**
  - **Код:** `src/features/common/pages/MainMenuPage.tsx:49-73,91-96,139-145`; `src/features/access-management/components/AppAccessGuard.tsx:45-70`.
  - Ошибка запроса только логируется; главное меню показывает «У вас нет доступа», guard молча возвращает в меню. Пользователь получает неправильное объяснение и не может повторить загрузку без обходных действий.
  - **Готово, когда:** состояние ошибки отдельно от запрета, есть retry и понятное объяснение; запрет остаётся fail-closed. Тесты network error, отсутствующий профиль и настоящий доступ false.

### UX-03
- [ ] **P2 — Не помечать важные уведомления прочитанными сразу после показа toast.**
  - **Код:** `src/features/water-monitoring/hooks/useWaterNotifications.tsx:19-42,58-81`; `ai-consultant-api/src/services/water/notificationService.ts:137-151`.
  - До 20 уведомлений показываются одновременно и сразу маркируются прочитанными, исчезая через 8–12 секунд. В просмотренном UI нет постоянного списка прочитанных уведомлений; новые сообщения при длительно открытой странице не опрашиваются. Ошибка обновления прочитанности в БД игнорируется.
  - **Готово, когда:** есть inbox/история, явное прочтение, ограничение количества toast и фоновое обновление; пользователь может вернуться к счёту/предупреждению. Сервер подтверждает фактическое изменение прочитанности.

### UX-04
- [ ] **P2 — Реализовать переход к счёту из push.**
  - **Код:** `public/sw.js:176-188`; `src/features/water-monitoring/pages/WaterPage.tsx:44-51`.
  - Service Worker открывает `/water?invoice=...`, но WaterPage обрабатывает только `tab`; других обработчиков параметра invoice в frontend не найдено. Кнопка «Открыть счёт» открывает обычный дашборд, не выбранный документ.
  - **Готово, когда:** deep link с однозначным ID открывает нужный счёт после авторизации; параметр не теряется при редиректе через login; тесты из закрытого PWA и уже открытой вкладки. Согласовать с DATA-11.

### UX-05
- [ ] **P1 — Сделать push-подписку управляемой и безопасной на общем устройстве.**
  - **Код:** `src/features/water-monitoring/hooks/usePushSubscription.ts:18-48`; `src/features/auth/contexts/AuthContext.tsx:609-624`; `ai-consultant-api/src/services/water/notificationService.ts:154-164`.
  - Разрешение запрашивается автоматически при открытии раздела, не явным действием; ошибки подписки скрыты. В frontend не найден сценарий unsubscribe, logout не удаляет/отвязывает подписку. На общем устройстве уведомления предыдущего пользователя могут приходить после выхода, пока подписка сохраняется.
  - **Готово, когда:** есть объяснение, кнопка включения, состояние и отключение; определена политика logout/смены аккаунта и реализована отвязка. Проверить denied, смену пользователя и iOS PWA, где пользовательский жест особенно важен.

### UX-06
- [ ] **P2 — Исправить клавиатурную и screen-reader доступность форм и модальных окон.**
  - **Код:** `src/features/common/components/AdminModal.tsx:18-40`; `src/features/equipment/components/MaintenanceLogModal.tsx:38-60`; `src/features/water-quality/components/WaterAnalysisForm.tsx:369-429,543-558`; `src/features/ai-consultant/components/ChatInput.tsx:105-114`.
  - Модальные окна имеют Escape/aria-modal, но нет начального фокуса, focus trap, возврата фокуса и доступного имени через заголовок. Многие label не связаны с input/select; textarea чата полагается на placeholder. Tab может уходить в фон, поля не имеют надёжного доступного имени.
  - **Готово, когда:** единый доступный Dialog, фон недоступен фокусу, закрытие возвращает фокус; у всех полей есть label/id или aria-label, у ошибок связь с полем. Добавить автоматические a11y-проверки и ручной прогон клавиатурой/screen reader.

### UX-07
- [ ] **P2 — Защищать несохранённый ввод от случайного закрытия.**
  - **Код:** `src/features/equipment/components/MaintenanceLogModal.tsx:38-55`; `src/features/water-quality/components/WaterAnalysisForm.tsx:334-339`; формы внутри журнала ТО.
  - Escape/клик по фону сразу размонтируют журнал вместе с формой; отмена анализа вызывает `navigate(-1)` без dirty-check. Ввод и выбранные файлы можно потерять одним случайным действием.
  - **Готово, когда:** формы сообщают dirty/saving-состояние контейнеру, пользователь подтверждает уход либо черновик восстанавливается; закрытие во время записи имеет определённое поведение. Проверить Escape, backdrop, Back и смену маршрута.

### UX-08
- [ ] **P1 — Не имитировать успешный logout при ошибке signOut.**
  - **Код:** `src/features/auth/services/supabaseAuthApi.ts:430-445`; `src/features/auth/contexts/AuthContext.tsx:609-624`.
  - API выбрасывает ошибку, но AuthContext её глотает и в finally очищает только своё состояние. Если Supabase не удалил локальную сессию, UI выглядит разлогиненным, а перезагрузка может восстановить пользователя. На общем терминале это опасное ложное подтверждение выхода.
  - **Готово, когда:** определён и проверен локальный выход при недоступности сервера, виден результат/ошибка отзыва удалённой сессии; повторная загрузка не возвращает аккаунт после подтверждённого выхода. Сбросить пользовательские кэши и подписки, не только профиль/путь.

## 5. PWA, инфраструктура и технический долг

### TECH-01
- [ ] **P1 — Сделать схему БД воспроизводимой с нуля и безопасной для обновления.**
  - **Код:** `supabase/migrations/20260224_add_device_role.sql:14`; `supabase/migrations/20260326_supabase_schema.sql:20-58`; `docs/migrations/*.sql`; `supabase/migrations/20260717_beliot_devices_registry.sql:346-386`.
  - Ранние миграции используют таблицы/профили до их создания базовой схемой 20260326. Сам baseline удаляет policies на ещё не созданных таблицах: `IF EXISTS` у policy не гарантирует существование таблицы. Часть обязательной схемы качества воды/участков/логов лежит только в docs. Повторный запуск baseline делает `DROP FUNCTION is_admin() CASCADE`, удаляя зависящие от неё policies более поздних фич, но восстанавливает только собственные.
  - **Готово, когда:** чистая временная БД поднимается одним документированным путём; upgrades и повторный запуск проверены без потери поздних grants/policies. Согласовать стратегию baseline/миграций с правилами проекта, не применять переписанную историю к production вслепую.

### TECH-02
- [ ] **P2 — Исправить offline-кэш PWA на Railway.**
  - **Код:** `public/sw.js:11-16,79-88,101-135`; `vite.config.ts:7-33`.
  - Исключение по `hostname.includes('railway.app')` пропускает не только API, но и собственные JS/CSS приложения на Railway-домене. Install предкэширует только `/`, `index.html`, manifest; заявленного в комментарии автоматического добавления assets в precache плагин не делает — он только меняет версию. После offline reload оболочке может не хватить JS.
  - **Готово, когда:** API исключаются по точным правилам, статические ресурсы своего origin кэшируются; стратегия lazy chunks и offline fallback определена. Проверить холодный/повторный offline-запуск production build на Railway origin и custom domain.

### TECH-03
- [ ] **P2 — Согласовать обновление Service Worker с жизненным циклом открытой страницы.**
  - **Код:** `public/sw.js:28-30,43-61`; `src/shared/utils/registerServiceWorker.ts:46-75,83-119`.
  - Worker автоматически вызывает skipWaiting/clients.claim и сразу удаляет старый кэш, хотя UI предлагает пользователю самому выбрать момент обновления. Страница старой версии может остаться со ссылками на удалённые lazy chunks. Activate также удаляет вообще все чужие Cache Storage caches того же origin.
  - **Готово, когда:** обновление ждёт согласованного момента, незавершённые формы защищены, обновление не ломает lazy navigation/offline. Удаляются только принадлежащие этому приложению кэши; тест двух открытых вкладок на разных версиях.

### TECH-04
- [ ] **P2 — Добавить автоматические quality gates и тесты критических сценариев.**
  - **Код/проверка:** `.github/workflows/collect-readings.yml`, `.github/workflows/invoice-sync.yml`; `package.json:13-16`; `vitest.config.ts:15-28`; `src/test/setup.ts:41-48`.
  - В репозитории найдены workflows интеграционных задач, но нет PR-проверок build/test/typecheck/lint. Frontend имеет 4 тестовых файла, backend — 9; рассмотренные пути HTTP-авторизации, RLS, водного баланса, атомарного сохранения и PWA ими не покрыты. `test:coverage` объявлен, но `@vitest/coverage-v8` не установлен. Все console.error/warn frontend-тестов безусловно заглушены.
  - **Готово, когда:** проверки выполняются на PR, coverage запускается воспроизводимо, неожиданные ошибки тестов видны; есть негативные тесты доступа, бизнес-фикстуры и минимальный browser E2E-набор. Цели покрытия задать для критических модулей, не только общим процентом.

### TECH-05
- [ ] **P2 — Разделить крупные UI/бизнес-модули и включить lint для типов и hooks.**
  - **Код:** `src/features/water-monitoring/components/WaterDashboard.tsx`; `src/features/auth/contexts/AuthContext.tsx`; `src/features/equipment/hooks/useEquipmentData.ts:101`; `ai-consultant-api/src/tools/waterTools.ts`; `vitest.config.ts:8`; оба `package.json`.
  - Дашборд в одном компоненте содержит запросы, агрегации, бизнес-исключения, состояние и разметку на тысячи строк. Есть `any`/касты и нарушенные dependency arrays при включённом strict; lint-скрипта/конфигурации не найдено. Это уже связано с дефектами DATA-01/02/05 и AI-03, а не только со стилем.
  - **Готово, когда:** расчёты вынесены в небольшие чистые функции с контрактными тестами, загрузки — в отдельные hooks/services, UI получает готовую модель; lint ловит unsafe types и exhaustive-deps. Рефакторить по частям после фиксации текущего поведения тестами, без массовой переписи.

### TECH-06
- [ ] **P2 — Согласовать локальные порты и CORS по умолчанию.**
  - **Код:** `vite.config.ts:73-79`; `ai-consultant-api/src/config/env.ts:50`.
  - Vite стартует на 3000, backend по умолчанию разрешает только origin на 5173. При занятости 3000 Vite выбирает другой порт, но `hmr.clientPort` остаётся 3000. Чистый запуск требует неочевидных ручных настроек и может ломать API/HMR.
  - **Готово, когда:** документированный default-запуск frontend/backend работает без CORS-ошибок, выбранные порты/HMR согласованы; origins валидируются и нормализуются, а не расширяются до произвольного `*`.

### TECH-07
- [ ] **P3 — Исправить несуществующий npm-скрипт и актуализировать описание рабочих интеграций.**
  - **Код:** `package.json:12`; `README.md:74-79,170-172,203-205`; `.github/workflows/invoice-sync.yml:3-17`; `ai-consultant-api/scripts/sync-invoices-scheduled.ps1`.
  - `inspect:posudotara` указывает на отсутствующий `scripts/inspect-posudotara-meter.ts`. README обещает ежедневную синхронизацию счетов через Actions, а workflow теперь только ручной и отсылает к Windows Scheduler; оборудование описано как legacy Sheets, хотя активно работает через GAS. Требования Node в README не согласованы с Vite 7/Node 20 Docker и Node 24 workflows. Документы-планы и заготовки (`osmos-collector`, repair requests) легко принять за готовые фичи.
  - **Готово, когда:** все документированные команды существуют и проверены, указана одна поддерживаемая матрица Node, актуальные источники данных/владельцы scheduled jobs; планы явно отделены от реализованного. Факт установки Windows-задачи проверить отдельно, не выводить из наличия ps1.

### TECH-08
- [ ] **P2 — Не компилировать тестовые файлы в production backend.**
  - **Код:** `ai-consultant-api/tsconfig.json:16-17`; `ai-consultant-api/Dockerfile:17-20,52-54`.
  - `include: src/**/*`, а exclude содержит только node_modules/dist: `*.test.ts` попадают в сборку вместе с декларациями. Production image получает весь dist при установке dependencies без devDependencies, хотя тестовые файлы импортируют Vitest.
  - **Готово, когда:** production tsconfig исключает тесты, отдельная проверка типизирует их; образ содержит только runtime-код. CI проверяет состав артефактов и запускает smoke test production-сервера.

### TECH-09
- [ ] **P1 — Привести async error handling и HTTP-статусы API к единому контракту.**
  - **Код:** `ai-consultant-api/src/index.ts:224-226`; `ai-consultant-api/src/routes/water/invoices.ts:167-196`; `ai-consultant-api/package.json:18`.
  - Общий handler превращает всё в 500, включая превышение body limit и ошибки JSON/multipart; пользователь не получает корректные 400/413. В download-route нет try/catch или async-wrapper для исключений await/arrayBuffer, а используется Express 4, который не автоматически передаёт rejected promises обработчику ошибок.
  - **Готово, когда:** все async endpoints передают исключения в error middleware, клиентские ошибки имеют корректный статус и безопасное сообщение, внутренние детали не уходят клиенту; тесты malformed JSON, oversized body/upload и исключения Storage.

### TECH-10
- [ ] **P2 — Сериализовать синхронизацию счетов и сделать уведомления идемпотентными.**
  - **Код:** `ai-consultant-api/src/routes/water/invoices.ts:45-75,113-158`; `ai-consultant-api/src/services/water/invoiceSyncService.ts:80-109,173-230`; `ai-consultant-api/src/services/water/notificationService.ts:32-44`.
  - `syncStatus.inProgress` защищает только async-ветку одного процесса. Обычные `/sync` и `/sync-all` вызывают сервис напрямую, а CLI/Windows Scheduler работают отдельно. Два запуска могут одновременно загрузить одинаковый набор existingKeys, повторно обработать счета и создать дубли уведомлений: upsert счёта не делает идемпотентной вставку notifications.
  - **Готово, когда:** все входы используют общую блокировку/job ID с восстановлением после сбоя; для уведомления задан уникальный ключ бизнес-события; повтор запуска не выдаётся за новый счёт. Тест параллельных sync и повторной обработки одного invoice.

## 6. Дополнительные ограничения и проверка развёртывания

Это обязательные проверки перед выводом о production-безопасности/качестве, а не уже доказанные эксплуатационные дефекты:

- Проверить реальные grants RPC/RLS и все обходные пути под тестовыми ролями после исправления SEC-04/05. SQL-файлы в репозитории не доказывают, какая схема применена сейчас.
- Проверить настройки публикации GAS и наличие внешнего auth gateway; не считать CORS защитой от прямых HTTP-запросов.
- Проверить reverse proxy и `trust proxy` для express-rate-limit: в исходниках trust proxy не настроен, поэтому за прокси лимиты могут считать общий адрес. Проверить лимиты по пользователю, общий бюджет AI и ограничения параллельных uploads.
- Проверить визуальные состояния на ширинах 320/375/768 px, масштаб 200%, длинные русские названия, таблицы/графики и PDF-печать. Без браузерного прогона проблемы контраста, перекрытий и размеров touch targets не объявляются подтверждёнными.
- Проверить camera/microphone permissions, iOS PWA, offline/reconnect и переключение аккаунтов на общем устройстве.
- Проверить реальные backup/restore, retention документов и логов, мониторинг свежести Beliot/счетов, доставку push, наличие и результат задачи Windows Scheduler. Наличие скрипта не подтверждает работоспособность расписания.
- Провести отдельное ревью вспомогательного MCP-сервера и его зависимостей: текущий dependency audit охватывал основной frontend-пакет и Express backend, не все подпроекты.

## Рекомендуемый порядок работ

1. SEC-01–05: закрыть границы доступа и риск сессии в Git, проверить исправления отрицательными тестами.
2. SEC-07–09 и DATA-01–06: TLS/зависимости, достоверность показаний и атомарность лабораторных данных.
3. Остальные DATA и AI: сохранение, расчёты, идентификация счетов, идемпотентность, отмена и ограничения вложений.
4. UX и PWA: достоверные состояния, уведомления, доступность, защита ввода и offline/update.
5. TECH: воспроизводимые миграции и CI выполнять параллельно с исправлениями; структурный рефакторинг — после регрессионных тестов.

Каждый пункт закрывать только после выполнения критерия готовности, а не после одного изменения кода. Изменения прав, отзыв сессий, удаление файлов из Git и любые операции с production согласовывать отдельно.
