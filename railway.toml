[build]
# Сборка: Dockerfile (BuildKit). Секреты Vite — см. комментарий в Dockerfile (secret vite_build_env).
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
