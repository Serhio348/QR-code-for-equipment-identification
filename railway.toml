[build]
# Сборка: Dockerfile. VITE_* — переменные сервиса Railway как build-time (см. Dockerfile).
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
