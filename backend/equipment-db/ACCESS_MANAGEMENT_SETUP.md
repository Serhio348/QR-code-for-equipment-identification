# Инструкция по добавлению модуля управления доступом

## Проблема

Если вы видите ошибку "Неизвестное действие" при попытке использовать функции управления доступом, это означает, что файл `AccessManagement.gs` не добавлен в проект Google Apps Script или код не обновлен.

## Решение

### Шаг 1: Добавьте файл AccessManagement.gs в проект

1. Откройте ваш проект Google Apps Script (Расширения → Apps Script)
2. В левой панели нажмите на значок "+" рядом с "Файлы"
3. Выберите "Скрипт"
4. Переименуйте новый файл в `AccessManagement.gs`
5. Скопируйте весь код из файла `backend/equipment-db/AccessManagement.gs` в этот новый файл
6. Сохраните файл (Ctrl+S или Cmd+S)

### Шаг 2: Обновите Code.gs

1. Откройте файл `Code.gs` в проекте Google Apps Script
2. Убедитесь, что обработчики добавлены в switch-case (строки 789-804):
   ```javascript
   case 'getAllUserAccess':
     Logger.log('🔐 Обработка getAllUserAccess');
     return handleGetAllUserAccess();
   
   case 'getUserAccess':
     Logger.log('🔐 Обработка getUserAccess');
     return handleGetUserAccess(data);
   
   case 'updateUserAccess':
     Logger.log('🔐 Обработка updateUserAccess');
     return handleUpdateUserAccess(data, data.adminEmail || null);
   ```
3. Убедитесь, что сообщение об ошибке обновлено (строка 910) и включает новые действия
4. Сохраните файл

### Шаг 3: Проверьте структуру проекта

Убедитесь, что в проекте Google Apps Script есть следующие файлы:
- `Code.gs` (главный файл)
- `AccessManagement.gs` (новый файл)
- `Utils.gs`
- `ResponseHelpers.gs`
- `UserManagement.gs`
- И другие необходимые модули

### Шаг 4: Сохраните и разверните

1. Сохраните все файлы (Ctrl+S)
2. Если нужно, обновите развертывание веб-приложения:
   - Развертывание → Управление развертываниями
   - Выберите активное развертывание
   - Нажмите "Редактировать"
   - Нажмите "Развернуть"

### Шаг 5: Проверка

После обновления кода попробуйте снова открыть страницу настроек доступа. Ошибка должна исчезнуть.

## Проверка логов

Если ошибка сохраняется:

1. Откройте Google Apps Script → Выполнения
2. Найдите последнее выполнение функции `doPost`
3. Проверьте логи - они покажут, какое действие было получено и почему оно не распознано

## Важно

- Все файлы модулей должны быть в одном проекте Google Apps Script
- Функции из разных файлов доступны глобально в проекте
- После добавления нового файла обязательно сохраните все изменения
- При необходимости обновите развертывание веб-приложения

