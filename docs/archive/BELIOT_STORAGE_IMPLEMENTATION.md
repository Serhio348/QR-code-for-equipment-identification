# Реализация хранения данных счетчиков Beliot

## Что реализовано

### ✅ Этап 1: localStorage (Реализовано)

**Файлы:**
- `src/utils/beliotStorage.ts` - утилиты для работы с localStorage
- `src/hooks/useBeliotDevicesStorage.ts` - React hook для управления данными

**Функциональность:**
- Сохранение пользовательских изменений (имя, адрес, серийный номер) в localStorage
- Быстрый доступ к данным
- Работа офлайн
- Автоматическая очистка старых данных (>90 дней)

---

### ✅ Этап 2: Google Sheets синхронизация (Реализовано)

**Файлы:**
- `backend/equipment-db/BeliotDevicesOverrides.gs` - модуль Google Apps Script
- `src/services/api/beliotDevicesStorageApi.ts` - API клиент
- Обновлен `backend/equipment-db/Code.gs` - добавлены endpoints

**Функциональность:**
- Создание листа "Счетчики Beliot" в Google Sheets
- Сохранение пользовательских изменений в Google Sheets
- Синхронизация между устройствами
- Резервное копирование данных

---

## Установка и настройка

### Шаг 1: Добавить модуль в Google Apps Script

1. Откройте ваш проект Google Apps Script (тот же, где находится `Code.gs`)
2. Создайте новый файл: **Файл → Создать → Файл скрипта**
3. Назовите файл: **BeliotDevicesOverrides**
4. Скопируйте содержимое из `backend/equipment-db/BeliotDevicesOverrides.gs`
5. Сохраните проект

### Шаг 2: Обновить Code.gs

1. Откройте `backend/equipment-db/Code.gs`
2. Убедитесь, что добавлены обработчики для счетчиков Beliot:
   - В `doGet`: `getBeliotDevicesOverrides`, `getBeliotDeviceOverride`
   - В `doPost`: `saveBeliotDeviceOverride`, `saveBeliotDevicesOverrides`, `deleteBeliotDeviceOverride`
3. Скопируйте обновленный код в Google Apps Script
4. Сохраните проект

### Шаг 3: Обновить развертывание

1. В Google Apps Script: **Развернуть → Управление развертываниями**
2. Нажмите на карандаш ✏️ рядом с существующим развертыванием
3. Убедитесь, что "У кого есть доступ" = **"Все"** ⚠️
4. Нажмите **Развернуть** (версия обновится автоматически)

### Шаг 4: Проверить работу

1. Откройте приложение в браузере
2. Перейдите на страницу счетчиков Beliot
3. Отредактируйте данные счетчика (двойной клик по ячейке)
4. Проверьте Google Sheets - должен появиться лист "Счетчики Beliot" с данными

---

## Структура данных

### localStorage

**Ключ:** `beliot_devices_overrides`

**Формат:**
```json
{
  "10596": {
    "name": "Новое имя",
    "address": "Новый адрес",
    "serialNumber": "13001660",
    "lastModified": 1705312800000
  }
}
```

### Google Sheets

**Лист:** "Счетчики Beliot"

**Колонки:**
- A: `deviceId` - ID устройства из Beliot API
- B: `name` - Пользовательское имя
- C: `address` - Пользовательский адрес
- D: `serialNumber` - Серийный номер
- E: `group` - Группа устройства
- F: `lastSync` - Дата последней синхронизации
- G: `lastModified` - Дата последнего изменения
- H: `modifiedBy` - Email пользователя

---

## API Endpoints

### GET запросы

#### Получить все изменения
```
GET ?action=getBeliotDevicesOverrides
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "10596": {
      "name": "Новое имя",
      "address": "Новый адрес",
      "serialNumber": "13001660",
      "lastModified": 1705312800000
    }
  }
}
```

#### Получить изменения для устройства
```
GET ?action=getBeliotDeviceOverride&deviceId=10596
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "name": "Новое имя",
    "address": "Новый адрес",
    "serialNumber": "13001660"
  }
}
```

### POST запросы

#### Сохранить изменения для устройства
```
POST
{
  "action": "saveBeliotDeviceOverride",
  "deviceId": "10596",
  "name": "Новое имя",
  "address": "Новый адрес",
  "serialNumber": "13001660",
  "modifiedBy": "user@example.com"
}
```

#### Сохранить несколько изменений
```
POST
{
  "action": "saveBeliotDevicesOverrides",
  "overrides": {
    "10596": {
      "name": "Новое имя",
      "address": "Новый адрес"
    },
    "10597": {
      "serialNumber": "13001661"
    }
  },
  "modifiedBy": "user@example.com"
}
```

#### Удалить изменения для устройства
```
POST
{
  "action": "deleteBeliotDeviceOverride",
  "deviceId": "10596"
}
```

---

## Алгоритм работы

### При загрузке приложения:

1. Загружаются устройства из Beliot API
2. Загружаются изменения из localStorage
3. Загружаются изменения из Google Sheets (синхронизация)
4. Применяются изменения с приоритетом:
   - localStorage (самые свежие локальные изменения)
   - Google Sheets (синхронизированные изменения)
   - Beliot API (исходные данные)

### При редактировании:

1. Сохранение в localStorage (мгновенно)
2. Синхронизация с Google Sheets (асинхронно, в фоне)
3. Обновление локального кэша синхронизированных данных

### При синхронизации:

1. Загрузка всех изменений из Google Sheets
2. Обновление локального кэша
3. Применение изменений к отображаемым данным

---

## Использование в компоненте

```typescript
import { useBeliotDevicesStorage } from '../hooks/useBeliotDevicesStorage';
import { getBeliotDevicesOverrides, saveBeliotDeviceOverride } from '../services/api/beliotDevicesStorageApi';

// В компоненте:
const { updateOverride, getOverride } = useBeliotDevicesStorage();
const [syncedOverrides, setSyncedOverrides] = useState({});

// Сохранение изменений
const saveOverride = async (deviceId, field, value) => {
  // Сохраняем в localStorage
  updateOverride(deviceId, field, value);
  
  // Синхронизируем с Google Sheets
  await saveBeliotDeviceOverride(deviceId, {
    [field]: value,
  });
};

// Получение значения с приоритетом
const getEditableValue = (deviceId, field, defaultValue) => {
  // 1. localStorage
  const local = getOverride(deviceId);
  if (local?.[field]) return local[field];
  
  // 2. Google Sheets
  const synced = syncedOverrides[deviceId];
  if (synced?.[field]) return synced[field];
  
  // 3. Значение по умолчанию
  return defaultValue;
};
```

---

## Тестирование

### Проверка localStorage:

1. Откройте DevTools (F12)
2. Перейдите на вкладку Application → Local Storage
3. Найдите ключ `beliot_devices_overrides`
4. Проверьте, что данные сохраняются при редактировании

### Проверка Google Sheets:

1. Откройте Google Sheets таблицу
2. Проверьте наличие листа "Счетчики Beliot"
3. Отредактируйте данные счетчика в приложении
4. Проверьте, что данные появились в таблице

### Проверка синхронизации:

1. Отредактируйте данные на одном устройстве
2. Откройте приложение на другом устройстве
3. Нажмите кнопку синхронизации 🔄
4. Проверьте, что изменения появились

---

## Устранение неполадок

### Данные не сохраняются в Google Sheets

1. Проверьте, что модуль `BeliotDevicesOverrides.gs` добавлен в проект
2. Проверьте, что endpoints добавлены в `Code.gs`
3. Проверьте логи в Google Apps Script: **Вид → Логи**
4. Убедитесь, что развертывание обновлено

### Ошибки синхронизации

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Проверьте, что URL API правильный в `src/config/api.ts`
3. Проверьте, что CORS настроен правильно ("У кого есть доступ" = "Все")

### Данные не отображаются

1. Проверьте, что `getEditableValue` использует правильные источники данных
2. Проверьте приоритет данных (localStorage > Google Sheets > API)
3. Проверьте консоль на наличие ошибок

---

## Следующие шаги

### Возможные улучшения:

1. **Автоматическая синхронизация** - периодическая синхронизация каждые 5-10 минут
2. **Обработка конфликтов** - при одновременном редактировании на разных устройствах
3. **История изменений** - сохранение истории всех изменений
4. **Уведомления** - уведомления о успешной/неуспешной синхронизации

---

## Поддержка

При возникновении проблем:
1. Проверьте логи в Google Apps Script
2. Проверьте консоль браузера
3. Проверьте документацию в `docs/BELIOT_STORAGE_RECOMMENDATION.md`

