# Тестирование Supabase

## 🧪 Быстрый тест создания и обновления пользователя

### Через Supabase Dashboard:

1. **Создание пользователя:**
   - Откройте **Supabase Dashboard** → **Authentication** → **Users**
   - Нажмите **Add user** → **Create new user**
   - Email: `test-user@example.com`
   - Password: `TestPassword123!`
   - ✅ **Auto Confirm User** (чтобы не требовать подтверждение email)
   - Нажмите **Create user**

2. **Проверка автоматического создания профиля:**
   - Откройте **Table Editor** → **profiles**
   - Должна быть создана запись с email `test-user@example.com`
   - Роль должна быть `user` по умолчанию

3. **Проверка автоматического создания доступа:**
   - Откройте **Table Editor** → **user_app_access**
   - Должна быть создана запись для нового пользователя
   - `equipment` и `water` должны быть `false`

### Через SQL (детальное тестирование):

Выполните скрипт `docs/TEST_USER_CRUD.sql` в **Supabase Dashboard** → **SQL Editor**

Этот скрипт проверит:
- ✅ Автоматическое создание профиля при регистрации
- ✅ Автоматическое создание доступа
- ✅ Обновление профиля пользователя
- ✅ Защиту от изменения роли обычными пользователями
- ✅ Работу функций `is_admin()` и `get_user_role()`
- ✅ RLS политики

---

# Тестирование Supabase

## 🔍 Быстрая проверка подключения

### 1. Проверка через браузер (консоль)

Откройте консоль браузера (F12) и выполните:

```javascript
// Проверка подключения к Supabase
import { supabase, testSupabaseConnection } from './src/config/supabase';

// Тест подключения
testSupabaseConnection().then(result => {
  console.log('✅ Подключение к Supabase:', result ? 'Успешно' : 'Ошибка');
});

// Проверка текущего пользователя
supabase.auth.getUser().then(({ data, error }) => {
  if (error) {
    console.error('❌ Ошибка получения пользователя:', error);
  } else {
    console.log('✅ Текущий пользователь:', data.user);
  }
});

// Проверка сессии
supabase.auth.getSession().then(({ data, error }) => {
  if (error) {
    console.error('❌ Ошибка получения сессии:', error);
  } else {
    console.log('✅ Сессия:', data.session ? 'Активна' : 'Отсутствует');
  }
});
```

### 2. Проверка через SQL Editor в Supabase Dashboard

1. Откройте **Supabase Dashboard** → **SQL Editor**
2. Выполните скрипт из файла `docs/CHECK_SUPABASE.sql`
3. Проверьте результаты всех секций

---

## 📋 Пошаговая проверка

### Шаг 1: Проверка переменных окружения

Убедитесь, что в `.env.local` или `.env` есть:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### Шаг 2: Проверка подключения

```bash
# В терминале проекта
npm run dev
```

Откройте консоль браузера и проверьте:
- Должно быть сообщение: `✅ Supabase клиент инициализирован`
- Не должно быть ошибок подключения

### Шаг 3: Проверка таблиц

В Supabase Dashboard → **Table Editor** проверьте наличие таблиц:
- ✅ `profiles`
- ✅ `user_app_access`
- ✅ `login_history`
- ✅ `beliot_device_overrides`

### Шаг 4: Проверка RLS политик

В Supabase Dashboard → **Authentication** → **Policies** проверьте:
- ✅ RLS включен на всех таблицах
- ✅ Политики созданы для всех операций (SELECT, INSERT, UPDATE)

### Шаг 5: Проверка функций

В Supabase Dashboard → **Database** → **Functions** проверьте:
- ✅ `is_admin()` - SECURITY DEFINER, STABLE
- ✅ `get_user_role(UUID)` - SECURITY DEFINER, STABLE
- ✅ `handle_new_user()` - SECURITY DEFINER
- ✅ `log_login(UUID, BOOLEAN, TEXT, TEXT)` - SECURITY DEFINER

### Шаг 6: Проверка триггеров

В Supabase Dashboard → **Database** → **Triggers** проверьте:
- ✅ `on_auth_user_created` на таблице `auth.users`
- ✅ Триггеры `update_*_updated_at` на таблицах

---

## 🧪 Тестирование функций

### Тест 1: Регистрация пользователя

1. Откройте страницу регистрации
2. Зарегистрируйте нового пользователя
3. Проверьте в Supabase Dashboard → **Table Editor** → `profiles`:
   - ✅ Создана запись в `profiles`
   - ✅ Создана запись в `user_app_access`
   - ✅ Роль по умолчанию: `user`

### Тест 2: Вход пользователя

1. Войдите под существующим пользователем
2. Проверьте в консоли браузера:
   - ✅ Нет ошибок аутентификации
   - ✅ Пользователь загружен
   - ✅ Роль определена правильно

### Тест 3: Проверка роли администратора

В Supabase Dashboard → **SQL Editor** выполните:

```sql
-- Назначить роль администратора (замените email)
UPDATE public.profiles 
SET role = 'admin' 
WHERE email = 'your-email@example.com';

-- Проверить роль
SELECT email, role FROM public.profiles WHERE email = 'your-email@example.com';
```

Затем:
1. Выйдите и войдите снова
2. Проверьте, что в главном меню появилась кнопка "Настройки доступа"

### Тест 4: Проверка доступа к приложениям

1. Войдите как администратор
2. Откройте "Настройки доступа"
3. Проверьте:
   - ✅ Таблица пользователей загружается
   - ✅ Можно включать/отключать доступ
   - ✅ Изменения сохраняются

---

## 🐛 Диагностика проблем

### Проблема: "Supabase клиент не инициализирован"

**Решение:**
1. Проверьте переменные окружения
2. Перезапустите dev сервер
3. Очистите кэш браузера

### Проблема: "Ошибка получения профиля"

**Решение:**
1. Проверьте RLS политики (выполните `CHECK_SUPABASE.sql`)
2. Убедитесь, что триггер `on_auth_user_created` создан
3. Проверьте функцию `handle_new_user()`

### Проблема: "infinite recursion detected in policy"

**Решение:**
1. Выполните скрипт `FIX_RLS_FINAL.sql`
2. Убедитесь, что политики SELECT разделены (без `is_admin()` в основной политике)

### Проблема: "Пользователь не найден после регистрации"

**Решение:**
1. Проверьте триггер `on_auth_user_created`
2. Проверьте функцию `handle_new_user()`
3. Проверьте политики INSERT для `profiles` и `user_app_access`

---

## ✅ Чеклист успешной настройки

- [ ] Переменные окружения настроены
- [ ] Supabase клиент инициализирован (нет ошибок в консоли)
- [ ] Все таблицы созданы
- [ ] RLS включен на всех таблицах
- [ ] Все политики созданы
- [ ] Все функции созданы и работают
- [ ] Все триггеры созданы
- [ ] Регистрация создает профиль автоматически
- [ ] Вход работает корректно
- [ ] Роль администратора определяется правильно
- [ ] Страница настроек доступа доступна администраторам

---

## 📞 Дополнительная помощь

Если что-то не работает:

1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи в Supabase Dashboard → **Logs**
3. Выполните диагностический SQL скрипт (`CHECK_SUPABASE.sql`)
4. Проверьте документацию: `docs/SUPABASE_SETUP.md`

