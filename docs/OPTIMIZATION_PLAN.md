# План оптимизации и рефакторинга

## 📊 Анализ текущего состояния

### Крупные файлы (требуют разбиения):
- `src/services/equipmentApi.ts` - **856 строк** ⚠️
- `src/components/EquipmentForm.tsx` - **804 строки** ⚠️
- `src/components/EquipmentList.tsx` - **269 строк** ✅ (нормально)
- `src/components/EquipmentPlate.tsx` - **~238 строк** ✅ (нормально)

### Проблемы:
1. **equipmentApi.ts** - монолитный файл со всей логикой API
2. **EquipmentForm.tsx** - большой компонент с множеством ответственностей
3. Дублирование кода форматирования дат в нескольких компонентах
4. Нет разделения на модули для лучшей читаемости

---

## 🎯 План оптимизации

### 1. Разбиение `equipmentApi.ts` (856 → ~150-200 строк на модуль)

#### Структура:
```
src/services/
├── api/
│   ├── apiRequest.ts          # Базовый запрос и CORS fallback (~150 строк)
│   ├── types.ts               # Типы для API (~50 строк)
│   ├── equipmentQueries.ts    # GET запросы (~100 строк)
│   ├── equipmentMutations.ts  # POST запросы (add, update, delete) (~300 строк)
│   └── driveApi.ts            # Google Drive API (~150 строк)
└── equipmentApi.ts            # Реэкспорт всех функций (~20 строк)
```

**Преимущества:**
- ✅ Легче найти нужную функцию
- ✅ Проще тестировать отдельные модули
- ✅ Меньше конфликтов при работе в команде
- ✅ Лучшая читаемость

---

### 2. Разбиение `EquipmentForm.tsx` (804 → ~200-300 строк)

#### Структура:
```
src/components/EquipmentForm/
├── EquipmentForm.tsx          # Основной компонент (~200 строк)
├── hooks/
│   └── useEquipmentForm.ts   # Логика формы (~200 строк)
├── components/
│   ├── SpecFields/
│   │   ├── FilterSpecFields.tsx
│   │   ├── PumpSpecFields.tsx
│   │   ├── TankSpecFields.tsx
│   │   ├── ValveSpecFields.tsx
│   │   ├── ElectricalSpecFields.tsx
│   │   ├── VentilationSpecFields.tsx
│   │   ├── PlumbingSpecFields.tsx
│   │   ├── IndustrialSpecFields.tsx
│   │   └── CommonSpecFields.tsx
│   └── FormFields.tsx        # Общие поля (название, тип, статус)
└── EquipmentForm.css
```

**Преимущества:**
- ✅ Каждый компонент полей характеристик ~50-80 строк
- ✅ Легче добавлять новые типы оборудования
- ✅ Переиспользование общих полей
- ✅ Логика формы отделена от UI

---

### 3. Вынос общих утилит

#### Новые файлы:
```
src/utils/
├── dateFormatting.ts          # Форматирование дат (используется в 3+ местах)
├── dateNormalization.ts       # Нормализация дат для input type="date"
└── validation.ts              # Валидация данных формы
```

**Текущее дублирование:**
- `formatDate()` - в EquipmentList, EquipmentPlate, EquipmentPage
- `normalizeDate()` - в EquipmentForm, EquipmentPage

**После рефакторинга:**
- Один источник правды для форматирования дат
- Легче поддерживать и исправлять баги

---

### 4. Оптимизация компонентов

#### EquipmentList.tsx
- Вынести `formatDate` в `utils/dateFormatting.ts`
- Вынести `getStatusBadge` в отдельный компонент `StatusBadge.tsx`

#### EquipmentPlate.tsx
- Вынести `formatDate` в `utils/dateFormatting.ts`
- Вынести логику рендеринга характеристик в отдельные компоненты

---

## 📋 Детальный план рефакторинга

### Этап 1: Утилиты (1-2 часа)
1. ✅ Создать `utils/dateFormatting.ts`
2. ✅ Создать `utils/dateNormalization.ts`
3. ✅ Заменить дублирующийся код в компонентах

**Результат:** -100-150 строк дублирующегося кода

---

### Этап 2: Разбиение equipmentApi.ts (2-3 часа)
1. ✅ Создать `services/api/apiRequest.ts` (базовый запрос)
2. ✅ Создать `services/api/types.ts` (типы)
3. ✅ Создать `services/api/equipmentQueries.ts` (GET запросы)
4. ✅ Создать `services/api/equipmentMutations.ts` (POST запросы)
5. ✅ Создать `services/api/driveApi.ts` (Google Drive)
6. ✅ Обновить `equipmentApi.ts` (реэкспорт)
7. ✅ Обновить импорты во всех компонентах

**Результат:** 
- equipmentApi.ts: 856 → ~20 строк (реэкспорт)
- Каждый модуль: ~100-300 строк
- Лучшая организация кода

---

### Этап 3: Разбиение EquipmentForm.tsx (3-4 часа)
1. ✅ Создать `hooks/useEquipmentForm.ts` (логика)
2. ✅ Создать `components/EquipmentForm/components/FormFields.tsx`
3. ✅ Создать `components/EquipmentForm/components/SpecFields/` (все типы)
4. ✅ Упростить `EquipmentForm.tsx` (только рендеринг)
5. ✅ Обновить импорты

**Результат:**
- EquipmentForm.tsx: 804 → ~200-300 строк
- Каждый компонент полей: ~50-80 строк
- Логика отделена от UI

---

### Этап 4: Мелкие оптимизации (1 час)
1. ✅ Вынести `StatusBadge` в отдельный компонент
2. ✅ Вынести константы типов оборудования в `constants/equipmentTypes.ts`
3. ✅ Создать хук `useEquipmentData` для загрузки и кеширования данных
4. ✅ Разбить `EquipmentPage.tsx` на компоненты (EquipmentPageHeader, DateEditor, StatusMessages)
5. ✅ Создать хук `useEquipmentDates` для управления датами

---

## 📈 Ожидаемые результаты

### До оптимизации:
- `equipmentApi.ts`: 856 строк
- `EquipmentForm.tsx`: 804 строки
- Дублирование кода: ~150 строк
- **Всего:** ~1810 строк в крупных файлах

### После оптимизации:
- `equipmentApi.ts`: ~20 строк (реэкспорт)
- Модули API: ~700 строк (разбито на 5 файлов)
- `EquipmentForm.tsx`: ~200-300 строк
- Компоненты полей: ~400-500 строк (разбито на 9 файлов)
- Утилиты: ~100 строк
- **Всего:** ~1420 строк (но лучше организовано)

### Преимущества:
- ✅ **-390 строк** общего кода (за счет устранения дублирования)
- ✅ Лучшая читаемость (маленькие модули)
- ✅ Проще поддерживать
- ✅ Легче тестировать
- ✅ Меньше конфликтов при работе в команде

---

## 🚀 Приоритеты

### Высокий приоритет:
1. **Утилиты** - быстро, большой эффект
2. **Разбиение equipmentApi.ts** - улучшает структуру проекта

### Средний приоритет:
3. **Разбиение EquipmentForm.tsx** - улучшает читаемость, но требует больше времени

### Низкий приоритет:
4. **Мелкие оптимизации** - можно делать постепенно

---

## 💡 Дополнительные рекомендации

### 1. Custom Hooks
Создать хуки для переиспользования логики:
- `useEquipmentData.ts` - загрузка и кеширование данных
- `useDateInput.ts` - работа с датами в формах

### 2. Константы
Вынести константы в отдельные файлы:
- `constants/equipmentTypes.ts` - типы оборудования и их названия
- `constants/formFields.ts` - конфигурация полей формы

### 3. Типы
Улучшить типизацию:
- Строгие типы для характеристик каждого типа оборудования
- Утилитарные типы для форм

---

## ⚠️ Важно

1. **Не ломать существующий функционал** - рефакторинг должен быть постепенным
2. **Тестировать после каждого этапа** - убедиться, что все работает
3. **Коммитить после каждого этапа** - легче откатить при проблемах
4. **Обновлять документацию** - описать новую структуру

---

## 📝 Чеклист для начала работы

- [ ] Создать ветку `refactor/optimization`
- [ ] Начать с Этапа 1 (утилиты) - самый безопасный
- [ ] Протестировать после каждого изменения
- [ ] Коммитить после каждого этапа
- [ ] Обновить документацию после завершения

