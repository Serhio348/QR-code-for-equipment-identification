# Варианты загрузки документации в папки оборудования

## Текущая ситуация

Каждое оборудование имеет свою папку в Google Drive, которая создается автоматически при создании оборудования. URL папки сохраняется в поле `googleDriveUrl` и используется для QR-кода.

## Варианты решения

### Вариант 1: Ручная загрузка через Google Drive UI ⭐ РЕКОМЕНДУЕТСЯ (самый простой)

**Описание:**
Пользователи загружают файлы напрямую в Google Drive через веб-интерфейс или приложение.

**Как это работает:**
1. В приложении на странице оборудования есть кнопка "Открыть папку в Google Drive"
2. Пользователь кликает на кнопку → открывается папка в новой вкладке
3. Пользователь загружает файлы через интерфейс Google Drive (drag & drop или кнопка "Загрузить")

**Преимущества:**
- ✅ Простота реализации (только кнопка с ссылкой)
- ✅ Не требует дополнительных API и разрешений
- ✅ Полный функционал Google Drive (версионирование, комментарии, совместный доступ)
- ✅ Нет ограничений на размер файлов (кроме лимитов Google Drive)
- ✅ Поддержка всех форматов файлов
- ✅ Можно организовать файлы в подпапки вручную

**Недостатки:**
- ❌ Нет контроля из приложения (не видно, какие файлы загружены)
- ❌ Нет автоматической организации файлов

**Реализация:**
```tsx
// На странице EquipmentPage добавить кнопку
<button onClick={() => window.open(equipment.googleDriveUrl, '_blank')}>
  📁 Открыть папку в Google Drive
</button>
```

---

### Вариант 2: Google Drive File Picker API

**Описание:**
Интеграция Google Drive File Picker в приложение для выбора и загрузки файлов из Google Drive.

**Как это работает:**
1. Пользователь нажимает "Загрузить документ" в приложении
2. Открывается окно выбора файлов из Google Drive
3. Пользователь выбирает файлы → они копируются в папку оборудования

**Преимущества:**
- ✅ Интеграция в приложение (не нужно покидать страницу)
- ✅ Можно выбирать файлы из любого места в Google Drive
- ✅ Официальный API от Google
- ✅ Поддержка множественного выбора

**Недостатки:**
- ❌ Требует настройки Google Cloud Project и OAuth
- ❌ Сложнее в реализации
- ❌ Требует авторизации пользователя
- ❌ Можно только копировать файлы, не загружать новые

**Реализация:**
- Требуется Google Cloud Project
- Настройка OAuth 2.0
- Использование Google Picker API
- Копирование файлов через Google Drive API

---

### Вариант 3: Drag & Drop с загрузкой через Google Apps Script

**Описание:**
Интерфейс drag & drop в приложении + загрузка файлов через Google Apps Script API.

**Как это работает:**
1. Пользователь перетаскивает файлы в область загрузки в приложении
2. Файлы загружаются на сервер (через Google Apps Script)
3. Google Apps Script сохраняет файлы в папку оборудования

**Преимущества:**
- ✅ Удобный интерфейс drag & drop
- ✅ Загрузка прямо из приложения
- ✅ Можно показывать прогресс загрузки
- ✅ Контроль над процессом загрузки

**Недостатки:**
- ❌ Требует реализации загрузки в Google Apps Script
- ❌ Ограничения на размер файла (через веб-интерфейс)
- ❌ Сложнее в реализации
- ❌ Требует обработки файлов на сервере

**Реализация:**
- Добавить компонент drag & drop в React
- Создать endpoint в Google Apps Script для загрузки файлов
- Использовать FormData для отправки файлов
- Обработка файлов через DriveApp в Google Apps Script

---

### Вариант 4: Прямая загрузка через Google Drive API v3

**Описание:**
Использование Google Drive API v3 напрямую из фронтенда для загрузки файлов.

**Как это работает:**
1. Пользователь выбирает файлы в приложении
2. Файлы загружаются напрямую в Google Drive через API
3. Файлы сохраняются в папку оборудования

**Преимущества:**
- ✅ Прямая загрузка без промежуточного сервера
- ✅ Полный контроль над процессом
- ✅ Можно показывать прогресс

**Недостатки:**
- ❌ Требует OAuth авторизации для каждого пользователя
- ❌ Сложная настройка (Google Cloud Project, OAuth)
- ❌ Ограничения безопасности (API ключи в клиенте)
- ❌ Сложнее в реализации

---

### Вариант 5: Комбинированный подход ⭐ РЕКОМЕНДУЕТСЯ (гибкий)

**Описание:**
Сочетание простого варианта (кнопка "Открыть папку") + дополнительный функционал для просмотра файлов.

**Как это работает:**
1. Основной способ: кнопка "Открыть папку в Google Drive" для загрузки файлов
2. Дополнительно: список файлов в папке (через Google Drive API или Google Apps Script)
3. Просмотр метаданных файлов (название, размер, дата загрузки)

**Преимущества:**
- ✅ Простота основной загрузки (через Google Drive UI)
- ✅ Дополнительный функционал просмотра файлов
- ✅ Можно добавить позже более сложные функции
- ✅ Гибкость в развитии

**Недостатки:**
- ❌ Требует API для получения списка файлов
- ❌ Немного сложнее, чем вариант 1

**Реализация:**
- Кнопка "Открыть папку" (простая ссылка)
- Функция в Google Apps Script для получения списка файлов
- Компонент для отображения списка файлов в приложении
- Возможность добавить позже drag & drop или другие функции

---

## Рекомендация

**Для начала: Вариант 1 (Ручная загрузка) + Вариант 5 (список файлов)**

Это самый простой и надежный подход:
1. Кнопка "Открыть папку в Google Drive" для загрузки файлов
2. Список файлов в папке (опционально, можно добавить позже)
3. При необходимости можно добавить drag & drop позже

**Почему это лучше:**
- Минимум кода и настройки
- Использует все возможности Google Drive
- Не требует сложной авторизации
- Легко расширяется в будущем

---

## Дополнительные возможности

### Организация файлов в подпапках

Можно автоматически создавать подпапки в папке оборудования:
- `Документация` - техническая документация
- `Сертификаты` - сертификаты и паспорта
- `Фото` - фотографии оборудования
- `Журнал обслуживания` - записи обслуживания

### Метаданные файлов

Можно хранить информацию о файлах в базе данных:
- Название файла
- Тип документа (паспорт, сертификат, инструкция и т.д.)
- Дата загрузки
- Размер файла

### Версионирование

Google Drive автоматически поддерживает версии файлов, можно показывать историю изменений.

---

## Следующие шаги

1. **Выберите вариант** (рекомендуется Вариант 1 или 5)
2. **Реализуйте базовый функционал** (кнопка "Открыть папку")
3. **Добавьте список файлов** (опционально, через Google Apps Script)
4. **Расширьте функционал** при необходимости (drag & drop, метаданные и т.д.)

