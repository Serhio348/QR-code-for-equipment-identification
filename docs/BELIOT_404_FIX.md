# Исправление ошибки 404 при получении устройств

## Проблема

Ошибка: `HTTP error! status: 404, message: {"error":{"msg":"Ресурс не найден!","code":4040}}`

## Причина

Endpoints передавались с префиксом `/api`, но `baseUrl` уже содержит `/api`, что приводило к неправильному URL:
- Неправильно: `https://beliot.by:4443/api/api/device/metering_devices`
- Правильно: `https://beliot.by:4443/api/device/metering_devices`

## Исправление

Все endpoints в `beliotDeviceApi.ts` обновлены для использования без префикса `/api`:

### До исправления:
```typescript
await beliotApiRequest('/api/device/metering_devices', 'POST', ...);
```

### После исправления:
```typescript
await beliotApiRequest('device/metering_devices', 'POST', ...);
```

## Обновленные endpoints

1. ✅ `device/metering_devices` - получение списка устройств
2. ✅ `device/metering_device/{id}` - получение устройства по ID
3. ✅ `abonent/main/data` - получение данных абонента
4. ✅ `device/{id}/state` - получение состояния устройства

## Проверка

После исправления:
1. Откройте консоль браузера (F12)
2. Перейдите на `/beliot-test`
3. Нажмите "Получить устройства компании"
4. Проверьте логи:
   - `🔍 Формирование URL:` - должен показать правильный URL
   - `🔑 Используется токен:` - должен показать наличие токена
   - `📤 Beliot API запрос:` - должен показать успешный запрос

## Ожидаемый результат

- ✅ Устройства должны загрузиться успешно
- ✅ В консоли не должно быть ошибок 404
- ✅ Должен отобразиться список устройств компании

