# Диагностика проблемы с журналом обслуживания на Railway

## Проблема
На Railway (продакшн) не отображаются записи в журнале обслуживания, хотя локально всё работает и в Google Drive таблице данные есть.

## Возможные причины

### 1. Переменная окружения `VITE_EQUIPMENT_API_URL` не установлена на Railway

**Проверка:**
1. Откройте проект на Railway
2. Перейдите в Settings → Variables
3. Проверьте, есть ли переменная `VITE_EQUIPMENT_API_URL`
4. Если её нет, добавьте её со значением (ваш URL из Google Apps Script):
   ```
   https://script.google.com/macros/s/AKfycbyp5X9iAUhLyocKlAWHLRaEt-S7RgbHX0ulgJQXcAEqNxNoSWbnQIdkbgWwV1RWpFm8TA/exec
   ```
   
   **ВАЖНО:** Используйте URL из вашего Google Apps Script проекта!
   Получить URL: Google Apps Script → Развернуть → Управление развертываниями → Скопировать URL веб-приложения

**Важно:** После добавления переменной нужно пересобрать проект (Redeploy).

### 2. Переменная окружения установлена неправильно

**Проверка:**
1. Убедитесь, что значение переменной начинается с `https://`
2. Убедитесь, что URL заканчивается на `/exec`
3. Убедитесь, что нет лишних пробелов или символов

### 3. Проблемы с CORS на Google Apps Script

**Проверка:**
1. Откройте Google Apps Script проект
2. Проверьте настройки развертывания:
   - Execute as: Me (ваш аккаунт)
   - Who has access: Anyone (или Anyone with Google account)
3. Убедитесь, что CORS включен в настройках

### 4. Проблемы с доступом к Google Sheets

**Проверка:**
1. Убедитесь, что Google Apps Script имеет доступ к таблице "Журнал обслуживания"
2. Проверьте, что скрипт может читать данные из таблицы
3. Проверьте логи выполнения в Google Apps Script (View → Executions)

## Диагностика через консоль браузера

После добавления логирования, откройте консоль браузера на Railway и проверьте:

1. **При загрузке журнала должны появиться логи:**
   ```
   📋 getMaintenanceLog вызвана: { equipmentId, maintenanceSheetId, apiUrl, ... }
   📤 Отправка запроса getMaintenanceLog: { params, url }
   📥 Получен ответ getMaintenanceLog: { success, dataLength, error }
   ```

2. **Если есть ошибки, проверьте:**
   - Тип ошибки (CORS, timeout, network error)
   - URL API (должен быть правильным)
   - Статус ответа (200, 404, 500, etc.)

3. **Если журнал пустой, проверьте:**
   - Есть ли записи в Google Sheets таблице
   - Правильный ли `equipmentId` (должен совпадать с ID в таблице)
   - Правильный ли `maintenanceSheetId` (если используется)

## Шаги для исправления

### Шаг 1: Проверьте переменные окружения на Railway

```bash
# Через Railway CLI (если установлен)
railway variables

# Или через веб-интерфейс
# Settings → Variables → VITE_EQUIPMENT_API_URL
```

### Шаг 2: Пересоберите проект

После изменения переменных окружения:
1. Перейдите в Deployments
2. Нажмите "Redeploy" или создайте новый deployment

### Шаг 3: Проверьте логи в консоли браузера

1. Откройте приложение на Railway
2. Откройте DevTools (F12)
3. Перейдите на вкладку Console
4. Откройте журнал обслуживания для любого оборудования
5. Проверьте логи в консоли

### Шаг 4: Проверьте логи Google Apps Script

1. Откройте Google Apps Script проект
2. Перейдите в View → Executions
3. Найдите последние выполнения функции `getMaintenanceLog`
4. Проверьте логи выполнения

## Дополнительная информация

- URL API должен быть установлен через переменную окружения `VITE_EQUIPMENT_API_URL`
- Получить URL: Google Apps Script → Развернуть → Управление развертываниями → Скопировать URL веб-приложения
- Переменная окружения: `VITE_EQUIPMENT_API_URL`
- Файл конфигурации: `src/config/api.ts`
- API функция: `src/services/api/maintenanceApi.ts`

## Если проблема не решена

1. Проверьте логи Railway (Deployments → View Logs)
2. Проверьте логи Google Apps Script (View → Executions)
3. Проверьте консоль браузера на Railway
4. Сравните переменные окружения локально и на Railway
5. Проверьте, что Google Apps Script развернут как веб-приложение с правильными настройками доступа

