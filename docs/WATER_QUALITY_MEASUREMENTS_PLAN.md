# План реализации функционала учета лабораторных измерений качества воды

## 1. Обзор функционала

Система учета лабораторных измерений качества воды для мониторинга и контроля параметров водоподготовки.

### Основные параметры измерения:
- **Железо** (Fe) - мг/л
- **Щелочность** - мг-экв/л или ммоль/л
- **Жесткость** - мг-экв/л или °Ж (градусы жесткости)
- **Окисляемость** - мг O₂/л

---

## 2. Структура данных

### 2.1. Таблица измерений (Supabase)

```sql
CREATE TABLE public.water_quality_measurements (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  equipment_id TEXT NOT NULL, -- Связь с оборудованием (фильтр, установка)
  measurement_date TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
  measured_by TEXT, -- Email или ID пользователя, который провел измерение
  
  -- Параметры измерения
  iron_value DECIMAL(10, 3), -- Железо, мг/л
  iron_unit TEXT DEFAULT 'мг/л',
  
  alkalinity_value DECIMAL(10, 3), -- Щелочность, мг-экв/л
  alkalinity_unit TEXT DEFAULT 'мг-экв/л',
  
  hardness_value DECIMAL(10, 3), -- Жесткость, мг-экв/л
  hardness_unit TEXT DEFAULT 'мг-экв/л',
  
  oxidizability_value DECIMAL(10, 3), -- Окисляемость, мг O₂/л
  oxidizability_unit TEXT DEFAULT 'мг O₂/л',
  
  -- Дополнительная информация
  notes TEXT, -- Примечания к измерению
  sample_location TEXT, -- Место отбора пробы (например, "После фильтра №1")
  temperature DECIMAL(5, 2), -- Температура воды при измерении, °C
  ph_value DECIMAL(4, 2), -- pH воды (опционально)
  
  -- Метаданные
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  created_by TEXT, -- Email пользователя
  
  -- Индексы
  CONSTRAINT fk_equipment FOREIGN KEY (equipment_id) 
    REFERENCES equipment(id) ON DELETE CASCADE
);

CREATE INDEX idx_measurement_date ON water_quality_measurements(measurement_date DESC);
CREATE INDEX idx_equipment_date ON water_quality_measurements(equipment_id, measurement_date DESC);
```

### 2.2. Таблица граничных значений (Supabase)

```sql
CREATE TABLE public.water_quality_limits (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  equipment_id TEXT, -- NULL для глобальных значений, или конкретное оборудование
  parameter_name TEXT NOT NULL, -- 'iron', 'alkalinity', 'hardness', 'oxidizability'
  
  -- Граничные значения
  min_value DECIMAL(10, 3), -- Минимальное допустимое значение
  max_value DECIMAL(10, 3), -- Максимальное допустимое значение
  warning_min DECIMAL(10, 3), -- Нижний порог предупреждения
  warning_max DECIMAL(10, 3), -- Верхний порог предупреждения
  
  unit TEXT NOT NULL, -- Единица измерения
  
  -- Настройки оповещений
  enable_notifications BOOLEAN DEFAULT true,
  notification_threshold_percent DECIMAL(5, 2) DEFAULT 10.0, -- Процент от границы для предупреждения
  
  -- Метаданные
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  created_by TEXT,
  
  UNIQUE(equipment_id, parameter_name)
);

-- Примеры данных по умолчанию
INSERT INTO water_quality_limits (equipment_id, parameter_name, min_value, max_value, warning_min, warning_max, unit) VALUES
  (NULL, 'iron', 0.0, 0.3, 0.05, 0.25, 'мг/л'),
  (NULL, 'alkalinity', 0.5, 6.5, 1.0, 5.5, 'мг-экв/л'),
  (NULL, 'hardness', 1.0, 7.0, 1.5, 6.0, 'мг-экв/л'),
  (NULL, 'oxidizability', 0.0, 5.0, 1.0, 4.0, 'мг O₂/л');
```

### 2.3. Таблица истории оповещений (Supabase)

```sql
CREATE TABLE public.water_quality_alerts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  measurement_id UUID NOT NULL,
  equipment_id TEXT NOT NULL,
  parameter_name TEXT NOT NULL,
  alert_type TEXT NOT NULL, -- 'warning', 'critical', 'below_min', 'above_max'
  current_value DECIMAL(10, 3) NOT NULL,
  limit_value DECIMAL(10, 3) NOT NULL,
  message TEXT NOT NULL,
  acknowledged BOOLEAN DEFAULT false,
  acknowledged_at TIMESTAMP WITH TIME ZONE,
  acknowledged_by TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  
  CONSTRAINT fk_measurement FOREIGN KEY (measurement_id) 
    REFERENCES water_quality_measurements(id) ON DELETE CASCADE
);

CREATE INDEX idx_alerts_unacknowledged ON water_quality_alerts(acknowledged, created_at DESC);
```

---

## 3. Функциональные требования

### 3.1. Ввод измерений

**Компоненты:**
- `WaterQualityMeasurementForm.tsx` - форма ввода измерений
- `WaterQualityMeasurementModal.tsx` - модальное окно для быстрого ввода
- API функции в `src/services/api/waterQualityApi.ts`

**Функционал:**
- Ввод всех параметров с валидацией
- Выбор оборудования (фильтра/установки)
- Автозаполнение предыдущих значений для сравнения
- Возможность прикрепить фото/файл результата анализа
- Сохранение черновика

### 3.2. Журнал наблюдений

**Компоненты:**
- `WaterQualityJournalPage.tsx` - страница журнала
- `WaterQualityMeasurementTable.tsx` - таблица измерений
- `WaterQualityChart.tsx` - графики динамики параметров

**Функционал:**
- Таблица с фильтрами:
  - По оборудованию
  - По дате (диапазон)
  - По параметру
  - По статусу (норма/предупреждение/критично)
- Сортировка по дате, оборудованию, параметрам
- Экспорт в Excel/PDF
- Поиск по примечаниям
- Группировка по оборудованию

### 3.3. Графики и аналитика

**Компоненты:**
- `WaterQualityChart.tsx` - графики с использованием Chart.js или Recharts
- `WaterQualityStatistics.tsx` - статистика

**Функционал:**
- Графики динамики каждого параметра во времени
- Сравнение нескольких единиц оборудования
- Отображение граничных значений на графиках
- Тренды (улучшение/ухудшение)
- Средние значения за период
- Минимум/максимум за период

### 3.4. Система оповещений

**Компоненты:**
- `WaterQualityAlertsPage.tsx` - страница оповещений
- `WaterQualityAlertNotification.tsx` - компонент уведомления
- Функция проверки границ в `src/utils/waterQualityAlerts.ts`

**Логика оповещений:**
1. **Предупреждение (Warning)** - значение приближается к границе:
   - В пределах 10-20% от граничного значения
   - Пример: max = 0.3, warning_max = 0.25, значение = 0.27 → предупреждение

2. **Критическое (Critical)** - значение превысило границу:
   - Выше max_value или ниже min_value
   - Требует немедленного внимания

3. **Тренд-оповещения**:
   - Быстрое ухудшение параметра (например, железо выросло на 50% за неделю)
   - Стабильное превышение нормы (3+ измерения подряд)

**Типы уведомлений:**
- Toast-уведомления в реальном времени
- Email-уведомления (опционально)
- Страница активных оповещений
- История оповещений

---

## 4. Дополнительная логика на основе данных измерений

### 4.1. Прогнозирование и рекомендации

**Компонент:** `WaterQualityRecommendations.tsx`

**Логика:**
- **Прогноз срока замены фильтрующего материала:**
  - Анализ тренда увеличения железа/окисляемости
  - Расчет оставшегося ресурса на основе скорости загрязнения
  - Рекомендация даты замены

- **Рекомендации по настройке оборудования:**
  - Если жесткость высокая → рекомендация увеличить дозу реагента
  - Если железо растет → рекомендация проверить/заменить фильтр
  - Если щелочность низкая → рекомендация по корректировке pH

- **Сравнение с нормативными значениями:**
  - СанПиН, ГОСТ для питьевой воды
  - Автоматическое сравнение и выделение отклонений

### 4.2. Отчетность

**Компоненты:**
- `WaterQualityReportsPage.tsx`
- `WaterQualityReportGenerator.tsx`

**Типы отчетов:**
1. **Ежедневный отчет:**
   - Все измерения за день
   - Статус каждого параметра
   - Количество оповещений

2. **Недельный/месячный отчет:**
   - Средние значения параметров
   - Динамика изменений
   - Графики трендов
   - Количество и типы оповещений
   - Рекомендации

3. **Отчет по оборудованию:**
   - История измерений конкретного фильтра/установки
   - Эффективность работы
   - Частота обслуживания

4. **Сравнительный отчет:**
   - Сравнение нескольких единиц оборудования
   - Выявление проблемных участков

### 4.3. Интеграция с планом обслуживания

**Логика:**
- Автоматическое создание задач обслуживания при критических значениях
- Связь измерений с записями обслуживания
- Отслеживание эффективности обслуживания (до/после)

**Пример:**
```
Измерение: Fe = 0.35 мг/л (критично, max = 0.3)
→ Автоматическое создание задачи: "Проверить/заменить фильтр обезжелезивания"
→ После обслуживания: новое измерение Fe = 0.15 мг/л
→ Система фиксирует улучшение и связывает с записью обслуживания
```

### 4.4. Статистический анализ

**Компонент:** `WaterQualityAnalytics.tsx`

**Метрики:**
- **Процент соответствия норме:**
  - Сколько % измерений в норме за период
  - Тренд улучшения/ухудшения

- **Частота превышений:**
  - Какие параметры чаще всего выходят за границы
  - Сезонность проблем

- **Корреляционный анализ:**
  - Связь между параметрами (например, железо и окисляемость)
  - Выявление закономерностей

- **Сравнение с историческими данными:**
  - "В этом месяце железо в среднем на 15% выше, чем в прошлом месяце"

### 4.5. Автоматические расчеты

**Логика:**
- **Индекс качества воды:**
  - Комплексный показатель на основе всех параметров
  - Шкала: отличное/хорошее/удовлетворительное/плохое

- **Скорость изменения параметров:**
  - Расчет скорости роста/снижения значений
  - Прогноз, когда будет достигнута граница

- **Эффективность очистки:**
  - Сравнение "до фильтра" и "после фильтра"
  - Расчет процента очистки

### 4.6. Напоминания и задачи

**Логика:**
- Автоматические напоминания о необходимости проведения измерений
- Настройка периодичности измерений для каждого оборудования
- Уведомления о пропущенных измерениях

---

## 5. Структура файлов проекта

```
src/
├── pages/
│   ├── WaterQualityJournalPage.tsx          # Журнал измерений
│   ├── WaterQualityAlertsPage.tsx           # Страница оповещений
│   └── WaterQualityReportsPage.tsx          # Отчеты
│
├── components/
│   ├── WaterQuality/
│   │   ├── WaterQualityMeasurementForm.tsx  # Форма ввода
│   │   ├── WaterQualityMeasurementTable.tsx # Таблица измерений
│   │   ├── WaterQualityChart.tsx            # Графики
│   │   ├── WaterQualityAlertNotification.tsx # Уведомления
│   │   ├── WaterQualityStatistics.tsx       # Статистика
│   │   ├── WaterQualityRecommendations.tsx  # Рекомендации
│   │   └── WaterQualityReportGenerator.tsx  # Генератор отчетов
│
├── services/
│   └── api/
│       └── waterQualityApi.ts               # API функции
│
├── hooks/
│   ├── useWaterQualityMeasurements.ts       # Хук для загрузки измерений
│   ├── useWaterQualityAlerts.ts             # Хук для оповещений
│   └── useWaterQualityLimits.ts             # Хук для граничных значений
│
├── utils/
│   ├── waterQualityAlerts.ts                # Логика проверки границ
│   ├── waterQualityCalculations.ts          # Расчеты и аналитика
│   └── waterQualityFormatters.ts            # Форматирование значений
│
└── types/
    └── waterQuality.ts                      # TypeScript типы
```

---

## 6. Этапы реализации

### Этап 1: Базовая инфраструктура (1-2 недели)
- [ ] Создание таблиц в Supabase
- [ ] Миграции SQL
- [ ] TypeScript типы
- [ ] Базовые API функции (CRUD)
- [ ] Базовая форма ввода измерений

### Этап 2: Журнал и отображение (1-2 недели)
- [ ] Страница журнала измерений
- [ ] Таблица с фильтрами и сортировкой
- [ ] Базовые графики динамики
- [ ] Интеграция с существующими страницами оборудования

### Этап 3: Система оповещений (1 неделя)
- [ ] Логика проверки граничных значений
- [ ] Компонент оповещений
- [ ] Страница активных оповещений
- [ ] Toast-уведомления

### Этап 4: Аналитика и отчеты (1-2 недели)
- [ ] Расширенные графики
- [ ] Статистика и метрики
- [ ] Генератор отчетов
- [ ] Экспорт в Excel/PDF

### Этап 5: Дополнительная логика (1-2 недели)
- [ ] Рекомендации и прогнозы
- [ ] Интеграция с планом обслуживания
- [ ] Автоматические расчеты
- [ ] Напоминания о измерениях

### Этап 6: Тестирование и оптимизация (1 неделя)
- [ ] Тестирование всех функций
- [ ] Оптимизация производительности
- [ ] Документация
- [ ] Обучение пользователей

---

## 7. UI/UX предложения

### 7.1. Цветовая индикация
- 🟢 **Зеленый** - значение в норме
- 🟡 **Желтый** - предупреждение (приближение к границе)
- 🟠 **Оранжевый** - близко к критическому
- 🔴 **Красный** - критическое значение (превышение границы)

### 7.2. Дашборд
- Виджеты с последними измерениями
- Графики трендов за последний месяц
- Активные оповещения
- Быстрый ввод нового измерения

### 7.3. Мобильная версия
- Удобный ввод измерений с телефона
- Просмотр графиков
- Получение push-уведомлений

---

## 8. Дополнительные идеи

### 8.1. Интеграция с внешними системами
- Импорт данных из лабораторных приборов (если есть API)
- Интеграция с системами SCADA
- Экспорт данных для внешней отчетности

### 8.2. Машинное обучение (опционально, будущее)
- Предсказание значений на основе исторических данных
- Выявление аномалий
- Автоматическая оптимизация граничных значений

### 8.3. Многопользовательский доступ
- Разные роли: оператор, лаборант, администратор
- Права доступа к редактированию/просмотру
- Аудит изменений

### 8.4. Шаблоны измерений
- Предустановленные наборы параметров для разных типов оборудования
- Быстрый ввод по шаблону

---

## 9. Технические детали

### 9.1. Используемые библиотеки
- **Графики:** Recharts или Chart.js
- **Таблицы:** react-table или ag-grid
- **Формы:** react-hook-form
- **Экспорт:** xlsx (Excel), jsPDF (PDF)
- **Даты:** date-fns

### 9.2. Производительность
- Пагинация для больших таблиц
- Виртуализация списков
- Кеширование данных
- Оптимизация запросов к Supabase

### 9.3. Безопасность
- RLS политики в Supabase
- Валидация данных на клиенте и сервере
- Защита от SQL-инъекций (через Supabase)
- Логирование всех изменений

---

## 10. Примеры использования

### Сценарий 1: Ежедневное измерение
1. Оператор открывает страницу оборудования
2. Нажимает "Добавить измерение"
3. Вводит значения: Fe=0.15, жесткость=3.2, щелочность=4.1, окисляемость=2.3
4. Система автоматически проверяет границы
5. Все в норме → сохранение
6. Появляется запись в журнале

### Сценарий 2: Обнаружение проблемы
1. Оператор вводит измерение: Fe=0.28 мг/л
2. Система проверяет: max=0.3, warning_max=0.25
3. Значение 0.28 > 0.25 → создается оповещение "Предупреждение: железо близко к границе"
4. Toast-уведомление появляется
5. Запись в журнале помечена желтым цветом
6. В разделе "Оповещения" появляется новая запись

### Сценарий 3: Генерация отчета
1. Администратор открывает "Отчеты"
2. Выбирает период: последний месяц
3. Выбирает оборудование: "Фильтр обезжелезивания №1"
4. Нажимает "Сгенерировать отчет"
5. Система создает PDF с:
   - Таблицей всех измерений
   - Графиками динамики
   - Статистикой
   - Рекомендациями

---

## 11. Приоритеты реализации

### Высокий приоритет (MVP)
1. ✅ Ввод измерений
2. ✅ Журнал наблюдений
3. ✅ Базовые оповещения
4. ✅ Простые графики

### Средний приоритет
1. Расширенная аналитика
2. Отчеты
3. Рекомендации
4. Интеграция с обслуживанием

### Низкий приоритет (будущее)
1. Машинное обучение
2. Интеграция с внешними системами
3. Мобильное приложение
4. Продвинутая статистика

---

## 12. Вопросы для уточнения

1. Какие единицы измерения используются на предприятии?
2. Как часто проводятся измерения? (ежедневно, еженедельно)
3. Кто будет вводить данные? (лаборанты, операторы)
4. Нужны ли email-уведомления?
5. Какие нормативные документы используются? (СанПиН, ГОСТ)
6. Нужна ли интеграция с существующими системами?
7. Какие отчеты требуются для руководства/контролирующих органов?

---

**Дата создания:** 2024
**Версия:** 1.0
**Статус:** План
