# Исправление ошибок аутентификации

## Проблемы и решения

### 1. ⚠️ Service Role key в клиентском коде

**Проблема:** Service Role key обнаружен в клиентском коде - это серьезная проблема безопасности!

**Решение:**
1. Откройте файл `.env.local` в корне проекта
2. Удалите или закомментируйте строку:
   ```env
   # VITE_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key  # УДАЛЕНО - не используется в клиенте
   ```
3. Service Role key должен использоваться ТОЛЬКО:
   - В GitHub Actions (через Secrets)
   - В Railway cron jobs (через Environment Variables)
   - В Supabase Edge Functions
   - В защищенном backend API

**Важно:** Service Role key НЕ должен попадать в клиентский код, так как он обходит все RLS политики и может быть раскрыт в браузере!

### 2. 🔐 Invalid Refresh Token

**Проблема:** `AuthApiError: Invalid Refresh Token: Refresh Token Not Found`

**Причина:** Старые или невалидные токены в localStorage

**Решение:**
1. Очистите localStorage в браузере:
   - Откройте DevTools (F12)
   - Перейдите в Application → Local Storage
   - Удалите все ключи, начинающиеся с `sb-auth-token`
   - Удалите ключ `user_session` (если есть)
2. Или выполните в консоли браузера:
   ```javascript
   localStorage.removeItem('sb-auth-token');
   localStorage.removeItem('sb-auth-token.0');
   localStorage.removeItem('sb-auth-token.1');
   localStorage.removeItem('user_session');
   ```
3. Перезагрузите страницу

**Автоматическое исправление:** Код теперь автоматически очищает невалидные токены при запуске приложения.

### 3. 🔄 Множественные инициализации

**Проблема:** Компоненты инициализируются несколько раз

**Причина:** React StrictMode в development режиме (это нормально)

**Решение:** Это не ошибка! React StrictMode намеренно вызывает двойную инициализацию в development для выявления проблем. В production этого не будет.

## Что было исправлено в коде

1. ✅ Удален `supabaseAdmin` клиент из клиентского кода
2. ✅ Добавлена автоматическая очистка невалидных токенов
3. ✅ Добавлена обработка ошибок refresh token в AuthContext
4. ✅ Добавлен PKCE flow для лучшей безопасности

## Проверка исправлений

После исправлений проверьте:

1. **Service Role key удален:**
   - Откройте `.env.local`
   - Убедитесь, что `VITE_SUPABASE_SERVICE_ROLE_KEY` закомментирован или удален
   - Перезапустите dev-сервер (`npm run dev`)

2. **Токены очищены:**
   - Очистите localStorage (см. выше)
   - Перезагрузите страницу
   - Войдите заново

3. **Ошибки исчезли:**
   - Проверьте консоль браузера
   - Не должно быть предупреждений о Service Role key
   - Не должно быть ошибок `Invalid Refresh Token`

## Дополнительная информация

- Service Role key используется только в серверных скриптах (`scripts/collect-beliot-readings.ts`)
- Для админских операций используйте RLS политики с проверкой роли через `is_admin()`
- Все токены теперь автоматически очищаются при обнаружении ошибок
