# Полный обзор приложения "Система идентификации оборудования"

**Дата создания:** 2024  
**Версия:** 2.0  
**Статус:** В активной разработке

---

## 📋 Содержание

1. [Общее описание](#общее-описание)
2. [Архитектура приложения](#архитектура-приложения)
3. [Основные модули](#основные-модули)
4. [Технологический стек](#технологический-стек)
5. [Структура базы данных](#структура-базы-данных)
6. [Маршрутизация](#маршрутизация)
7. [Система аутентификации и безопасности](#система-аутентификации-и-безопасности)
8. [Текущее состояние разработки](#текущее-состояние-разработки)
9. [Планы развития](#планы-развития)

---

## 🎯 Общее описание

Веб-приложение для управления оборудованием с генерацией табличек, QR-кодов, интеграцией с Google Drive и автоматическим сбором данных со счетчиков воды через Beliot API. Приложение также включает модуль учета лабораторных измерений качества воды.

### Основные возможности:

- **Управление оборудованием** - полный CRUD для оборудования с интеграцией Google Drive
- **Мониторинг счетчиков воды** - автоматический сбор и визуализация данных через Beliot API
- **Анализ качества воды** - система учета лабораторных измерений с нормативами и оповещениями
- **Управление доступом** - гибкая система контроля доступа к модулям приложения
- **Мониторинг ошибок** - централизованная система логирования и отслеживания ошибок
- **PWA** - поддержка установки как нативное приложение

---

## 🏗️ Архитектура приложения

### Frontend (React + TypeScript)

```
src/
├── components/          # Переиспользуемые React компоненты
├── pages/              # Страницы приложения (роуты)
├── services/           # API клиенты и бизнес-логика
│   └── api/            # API функции для работы с бэкендом
├── hooks/              # Кастомные React хуки
├── contexts/           # React контексты (Auth, etc.)
├── types/              # TypeScript типы и интерфейсы
├── utils/              # Утилиты и вспомогательные функции
├── config/             # Конфигурация (API endpoints, Supabase)
└── styles/             # Глобальные стили
```

### Backend

- **Supabase** - Backend-as-a-Service
  - PostgreSQL база данных
  - Supabase Auth для аутентификации
  - Row Level Security (RLS) для безопасности
  - Storage для файлов
  
- **Google Apps Script** - Legacy API для работы с оборудованием
  - Google Sheets как база данных
  - Google Drive API для документации

- **Beliot API** - Внешний API для получения данных со счетчиков воды

### Интеграции

- **GitHub Actions** - Автоматический сбор данных каждый час
- **Railway** - Хостинг приложения (опционально)
- **Docker** - Контейнеризация (опционально)

---

## 📦 Основные модули

### 1. Модуль управления оборудованием

**Функциональность:**
- Просмотр списка оборудования с фильтрацией
- Создание, редактирование, удаление оборудования
- Генерация QR-кодов и табличек
- Экспорт в PDF
- Интеграция с Google Drive для документации
- Журнал обслуживания

**Основные компоненты:**
- `EquipmentPage.tsx` - страница просмотра оборудования
- `EquipmentFormPage.tsx` - форма создания/редактирования
- `EquipmentList.tsx` - список оборудования
- `EquipmentPlate.tsx` - табличка оборудования
- `DriveFilesList.tsx` - список файлов из Google Drive
- `MaintenanceLog.tsx` - журнал обслуживания

**API:**
- `equipmentApi.ts` - клиент для Google Apps Script API
- `equipmentQueries.ts` - запросы к Supabase
- `equipmentMutations.ts` - мутации (создание, обновление, удаление)

### 2. Модуль мониторинга счетчиков воды (Beliot)

**Функциональность:**
- Просмотр текущих показаний счетчиков
- Архив данных с группировкой (час, день, неделя, месяц, год)
- Графики и диаграммы потребления
- Поиск устройств
- Паспорт устройства
- Автоматическая синхронизация через GitHub Actions

**Основные компоненты:**
- `WaterPage.tsx` - главная страница модуля
- `BeliotDevicesTest.tsx` - компонент мониторинга

**API:**
- `beliotReadingsApi.ts` - получение показаний
- `beliotDevicesStorageApi.ts` - кэширование данных
- `supabaseBeliotReadingsApi.ts` - работа с Supabase

**Автоматизация:**
- GitHub Actions workflow (`collect-readings.yml`)
- Запуск каждый час в 50 минут
- Сбор данных за последние 24 часа

### 3. Модуль анализа качества воды

**Функциональность:**
- Управление пунктами отбора проб
- Ввод лабораторных анализов
- Управление нормативами качества воды
- Автоматическая проверка соответствия нормативам
- Оповещения о превышении
- Журнал анализов с фильтрацией

**Основные компоненты:**

**Пункты отбора проб:**
- `SamplingPointsPage.tsx` - список точек
- `SamplingPointForm.tsx` - форма создания/редактирования
- `SamplingPointViewPage.tsx` - просмотр точки

**Анализы:**
- `WaterQualityJournalPage.tsx` - журнал анализов
- `WaterAnalysisForm.tsx` - форма ввода анализа
- `WaterAnalysisViewPage.tsx` - просмотр анализа

**Нормативы:**
- `WaterQualityNormsPage.tsx` - список нормативов
- `WaterQualityNormForm.tsx` - форма создания/редактирования
- `WaterQualityNormViewPage.tsx` - просмотр норматива

**Оповещения:**
- `WaterQualityAlertsPage.tsx` - страница оповещений

**API:**
- `samplingPoints.ts` - CRUD для точек отбора проб
- `waterAnalysis.ts` - CRUD для анализов
- `analysisResults.ts` - управление результатами измерений
- `waterQualityNorms.ts` - управление нормативами
- `compliance.ts` - проверка соответствия нормативам
- `alerts.ts` - управление оповещениями
- `incidents.ts` - управление инцидентами

**Параметры измерения:**
- Железо (Fe) - мг/л
- Щелочность - мг-экв/л
- Жесткость - мг-экв/л
- Окисляемость - мг O₂/л
- pH - единицы pH
- Температура - °C

### 4. Модуль управления доступом

**Функциональность:**
- Настройка доступа пользователей к модулям приложения
- Таблица доступа (десктоп)
- Выпадающий список (мобильные устройства)
- Роли: администратор / пользователь

**Основные компоненты:**
- `AccessSettingsPage.tsx` - страница настроек доступа

**API:**
- `supabaseAccessApi.ts` - управление доступом

### 5. Модуль мониторинга ошибок

**Функциональность:**
- Централизованное логирование ошибок
- Классификация по уровням серьезности
- Фильтрация по статусу
- Статистика ошибок
- Отметка ошибок как решенных

**Основные компоненты:**
- `ErrorLogsPage.tsx` - страница логов ошибок

**API:**
- `errorLogsApi.ts` - работа с логами ошибок
- `errorHandler.ts` - централизованная обработка ошибок

### 6. Модуль управления участками

**Функциональность:**
- Управление участками предприятия
- CRUD операции для участков

**Основные компоненты:**
- `WorkshopSettingsPage.tsx` - страница управления участками

**API:**
- `supabaseWorkshopApi.ts` - работа с участками

---

## 🛠️ Технологический стек

### Frontend

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **React Router DOM 7** - маршрутизация
- **Recharts** - графики и диаграммы
- **React Toastify** - уведомления
- **QRCode.react** - генерация QR-кодов
- **jsPDF + html2canvas** - экспорт в PDF
- **html5-qrcode** - сканирование QR-кодов

### Backend

- **Supabase**
  - PostgreSQL
  - Supabase Auth
  - Row Level Security (RLS)
  - Storage API
  
- **Google Apps Script** (Legacy)
  - Google Sheets API
  - Google Drive API

### Интеграции

- **Beliot API** - API счетчиков воды
- **GitHub Actions** - CI/CD и автоматизация

### DevOps

- **Railway** - хостинг (основная платформа)
- **Docker** - контейнеризация (используется для деплоя)
- **Nginx** - веб-сервер для production
- **Vite** - сборка для production
- **Nixpacks** - альтернативный билдер (Railway)

---

## 🗄️ Структура базы данных

### Основные таблицы

#### Оборудование (Legacy - Google Sheets)
- Хранится в Google Sheets через Google Apps Script API

#### Счетчики воды (Supabase)

**beliot_device_readings**
- Хранение показаний счетчиков
- Почасовые данные
- Автоматический сбор через GitHub Actions

**beliot_device_overrides**
- Переопределения данных устройств
- Паспортные данные

#### Качество воды (Supabase)

**sampling_points**
- Пункты отбора проб (точки контроля)
- Связь с оборудованием
- Настройки периодичности отбора

**water_analysis**
- Лабораторные анализы
- Связь с точкой отбора проб
- Статусы анализа
- Файлы (PDF)

**analysis_results**
- Результаты измерений параметров
- Нормализованная структура (один параметр = одна запись)
- Соответствие нормативам

**water_quality_norms**
- Нормативы качества воды
- Допустимые и предупреждающие диапазоны
- Привязка к точкам отбора проб

**water_quality_alerts**
- Оповещения о превышении нормативов
- Автоматическое создание через триггеры БД

**water_quality_incidents**
- Инциденты качества воды
- Группировка связанных оповещений

#### Системные таблицы (Supabase)

**users** (Supabase Auth)
- Пользователи системы
- Аутентификация

**user_access**
- Настройки доступа пользователей к модулям
- Роли и права

**workshops**
- Участки предприятия

**error_logs**
- Логи ошибок приложения
- Классификация и статусы

### Связи между таблицами

```
sampling_points (1) ──< (N) water_analysis
water_analysis (1) ──< (N) analysis_results
water_quality_norms (1) ──< (N) analysis_results (для проверки соответствия)
analysis_results (1) ──< (N) water_quality_alerts
water_quality_alerts (N) ──< (1) water_quality_incidents
```

---

## 🗺️ Маршрутизация

### Основные маршруты

**Аутентификация:**
- `/` - Главное меню
- `/login` - Вход
- `/register` - Регистрация
- `/reset-password` - Восстановление пароля

**Оборудование:**
- `/equipment` - Список оборудования
- `/equipment/new` - Создание оборудования
- `/equipment/:id` - Просмотр оборудования
- `/equipment/:id/edit` - Редактирование оборудования

**Вода (Beliot):**
- `/water` - Мониторинг счетчиков воды

**Качество воды:**
- `/water-quality/journal` - Журнал анализов
- `/water-quality/analysis/new` - Создание анализа
- `/water-quality/analysis/:id` - Просмотр анализа
- `/water-quality/analysis/:id/edit` - Редактирование анализа
- `/water-quality/norms` - Нормативы
- `/water-quality/norm/new` - Создание норматива
- `/water-quality/norm/:id` - Просмотр норматива
- `/water-quality/norm/:id/edit` - Редактирование норматива
- `/water-quality/sampling-points` - Точки отбора проб
- `/water-quality/sampling-point/new` - Создание точки
- `/water-quality/sampling-point/:id` - Просмотр точки
- `/water-quality/sampling-point/:id/edit` - Редактирование точки
- `/water-quality/alerts` - Оповещения

**Администрирование:**
- `/admin/access-settings` - Настройки доступа
- `/admin/error-logs` - Логи ошибок
- `/admin/workshop-settings` - Управление участками

---

## 🔐 Система аутентификации и безопасности

### Аутентификация

- **Supabase Auth** - управление пользователями
- Регистрация и вход через email/password
- Восстановление пароля через email
- Сессии (8 часов активности)

### Безопасность

- **Row Level Security (RLS)** - защита данных на уровне строк
- Политики доступа для каждой таблицы
- Разделение прав администраторов и пользователей
- Централизованная обработка ошибок

### Управление доступом

- Система ролей (администратор / пользователь)
- Гибкая настройка доступа к модулям
- Таблица `user_access` для хранения настроек

---

## 📊 Текущее состояние разработки

### ✅ Реализовано

#### Модуль оборудования
- ✅ Полный CRUD для оборудования
- ✅ Генерация QR-кодов и табличек
- ✅ Экспорт в PDF
- ✅ Интеграция с Google Drive
- ✅ Журнал обслуживания

#### Модуль мониторинга счетчиков
- ✅ Просмотр текущих показаний
- ✅ Архив данных с группировкой
- ✅ Графики и диаграммы
- ✅ Автоматический сбор данных
- ✅ Поиск устройств

#### Модуль качества воды (MVP)
- ✅ Управление пунктами отбора проб
- ✅ Ввод лабораторных анализов
- ✅ Управление нормативами
- ✅ Автоматическая проверка соответствия
- ✅ Оповещения о превышении
- ✅ Журнал анализов
- ✅ Фильтрация по точкам отбора проб
- ✅ Удаление анализов

#### Системные модули
- ✅ Аутентификация и авторизация
- ✅ Управление доступом
- ✅ Мониторинг ошибок
- ✅ Управление участками

### ⏳ В разработке

#### Модуль качества воды (расширенный функционал)
- ⏳ Графики динамики параметров
- ⏳ Статистика и аналитика
- ⏳ Карты Шухарта
- ⏳ Интегральные показатели (LSI, RSI)
- ⏳ Планирование отбора проб
- ⏳ Расширенная отчетность

### 📝 Планируется

- Корреляционный анализ
- Прогнозирование
- Интеграция с системами управления
- Мобильное приложение

---

## 🚀 Планы развития

### Приоритет 1 (Высокий)

1. **Графики динамики параметров** (1-2 недели)
   - Базовые графики с использованием Recharts
   - Отображение нормативов на графиках
   - Интеграция в страницы анализа и журнала

2. **Статистика и аналитика** (2-3 недели)
   - Средние значения параметров
   - Тренды и изменения
   - Сравнение периодов

### Приоритет 2 (Средний)

1. **Карты Шухарта** (3-4 недели)
   - Контрольные карты для параметров
   - Выявление трендов и аномалий

2. **Интегральные показатели** (2-3 недели)
   - Индекс Ланжелье (LSI)
   - Индекс Райзнера (RSI)
   - Автоматический расчет

### Приоритет 3 (Низкий)

1. **Планирование отбора проб** (2-3 недели)
   - Календарь отбора проб
   - Напоминания
   - Автоматическое создание записей

2. **Расширенная отчетность** (3-4 недели)
   - Генерация отчетов
   - Экспорт в различные форматы
   - Шаблоны отчетов

---

## 🐳 Контейнеризация и деплой

### Использование Docker

Приложение **использует Docker** для контейнеризации и деплоя на платформу **Railway**.

**Важно:** В файле `railway.json` явно указан `"builder": "DOCKERFILE"`, что означает, что Railway использует Dockerfile для сборки. Если бы этого не было, Railway автоматически использовал бы Nixpacks для сборки.

### Архитектура Docker образа

**Многоступенчатая сборка (Multi-stage build):**

#### Stage 1: Builder
- **Базовый образ:** `node:20-alpine`
- **Задачи:**
  - Установка зависимостей (`npm ci`)
  - Копирование исходного кода
  - Создание `.env` файла из build args
  - Сборка приложения (`npm run build`)

#### Stage 2: Production
- **Базовый образ:** `node:20-alpine`
- **Установка:** Nginx для раздачи статических файлов
- **Содержимое:**
  - Собранные файлы из `dist/` → `/usr/share/nginx/html`
  - Исходный код и зависимости для cron jobs
  - Nginx конфигурация
  - Entrypoint скрипт

### Файлы контейнеризации

**Dockerfile**
- Многоступенчатая сборка
- Оптимизация размера образа (alpine)
- Поддержка build args для переменных окружения

**docker-entrypoint.sh**
- Скрипт запуска контейнера
- Настройка Nginx
- Запуск веб-сервера на порту 80
- Логирование для отладки

**nginx.conf.template**
- Шаблон конфигурации Nginx
- Поддержка SPA роутинга
- Кэширование статических файлов
- Gzip сжатие
- Security headers

**railway.json**
- Конфигурация для Railway
- Использование Dockerfile как билдера
- Настройка cron jobs (сбор данных Beliot)
- Политика перезапуска

**nixpacks.toml**
- Альтернативная конфигурация для Nixpacks
- Используется Railway, если Dockerfile недоступен

### Процесс деплоя

1. **Сборка образа:**
   ```bash
   docker build -t app .
   ```

2. **Локальный запуск:**
   ```bash
   docker run -p 8080:80 app
   ```

3. **На Railway:**
   - Railway автоматически обнаруживает Dockerfile (благодаря `railway.json` с `"builder": "DOCKERFILE"`)
   - Автоматическая сборка при push в репозиторий
   - Переменные окружения, настроенные в Railway Dashboard, автоматически передаются как build args
   - Автоматический деплой после успешной сборки

### Особенности

- **Переменные окружения:** Railway автоматически передает env vars как build args
- **Порт:** Контейнер слушает порт 80, Railway проксирует на внешний порт
- **Cron jobs:** Настроены через `railway.json` для автоматического сбора данных
- **SPA роутинг:** Nginx настроен для корректной работы React Router
- **Кэширование:** Оптимизировано кэширование статических ресурсов

### Альтернативные варианты деплоя

1. **Nixpacks** (Railway) - НЕ используется в текущей конфигурации
   - Railway автоматически использует Nixpacks, если Dockerfile не найден
   - Файл `nixpacks.toml` присутствует как резервный вариант
   - **Текущая конфигурация:** Railway использует Dockerfile (указано в `railway.json`)

2. **Локальная сборка**
   ```bash
   npm run build
   npm start  # Использует serve для раздачи dist/
   ```

3. **Другие платформы**
   - Docker образ можно использовать на любой платформе
   - Требуется только Docker runtime

---

## 📁 Структура проекта

```
QR-code-for-equipment-identification/
├── src/                          # Frontend (React)
│   ├── components/              # React компоненты
│   ├── pages/                   # Страницы приложения
│   ├── services/                # API клиенты
│   │   └── api/                 # API функции
│   │       └── waterQuality/    # API качества воды
│   ├── hooks/                   # Кастомные хуки
│   ├── contexts/                # React контексты
│   ├── types/                   # TypeScript типы
│   ├── utils/                   # Утилиты
│   └── config/                  # Конфигурация
├── backend/                     # Backend (Google Apps Script)
│   └── equipment-db/           # API для оборудования
├── docs/                        # Документация
│   ├── migrations/             # SQL миграции
│   └── queries/                # SQL запросы
├── scripts/                     # Скрипты
├── .github/                     # GitHub Actions
└── public/                      # Статические файлы
```

---

## 🔧 Конфигурация

### Переменные окружения

```env
# Supabase (обязательно)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Google Apps Script (для оборудования)
VITE_EQUIPMENT_API_URL=https://script.google.com/...

# Beliot API (для счетчиков воды)
VITE_BELIOT_API_BASE_URL=https://beliot.by:4443/api
VITE_BELIOT_LOGIN=your-login
VITE_BELIOT_API_KEY=your-api-key
```

---

## 📚 Дополнительная документация

- [README.md](../README.md) - Основная документация
- [WATER_QUALITY_DEVELOPMENT_ROADMAP.md](./WATER_QUALITY_DEVELOPMENT_ROADMAP.md) - План развития модуля качества воды
- [WATER_QUALITY_MEASUREMENTS_PLAN_V2.md](./WATER_QUALITY_MEASUREMENTS_PLAN_V2.md) - Детальный план реализации
- [TESTING.md](./TESTING.md) - Документация по тестированию
- [ERROR_HANDLING.md](./ERROR_HANDLING.md) - Обработка ошибок

---

**Версия документа:** 1.0  
**Последнее обновление:** 2024
