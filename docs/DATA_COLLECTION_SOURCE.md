# Источник данных для сбора показаний

## 📊 Откуда собираются данные

Система собирает показания счетчиков из **Beliot API** - это внешний API сервиса Beliot, который предоставляет данные о счетчиках в реальном времени.

## 🔄 Процесс сбора данных

### 1. Источник данных: Beliot API

**Endpoint:** `POST /api/device/attributes`

**Запрос:**
```json
{
  "device_id": "11018"
}
```

**Ответ:** Атрибуты устройства, включая текущие показания счетчика

### 2. Извлечение показаний

Система ищет показания в следующих местах ответа API:

#### Вариант 1: Из атрибутов устройства (`device/attributes`)
- Ищет атрибуты с именами, содержащими: `in1`, `in2`, `reading`, `measurement`, `value`
- Извлекает значение из поля `value` или `attribute_value`
- Извлекает дату из полей `date`, `created_at`, `timestamp`
- Единица измерения: `unit` или по умолчанию `м³`

#### Вариант 2: Из данных устройства (`last_message_type`)
Если показания не найдены в атрибутах, система проверяет:
- `device.last_message_type['1'].in1` - текущее показание
- `device.last_message_type['2'].in1` - предыдущее показание

### 3. Пример структуры данных из Beliot API

```json
{
  "data": [
    {
      "attribute_name": "in1",
      "value": 5401.37,
      "attribute_value": 5401.37,
      "date": "2024-01-15T10:30:00Z",
      "created_at": "2024-01-15T10:30:00Z",
      "unit": "м³"
    }
  ]
}
```

Или:

```json
{
  "last_message_type": {
    "1": {
      "in1": 5401.37,
      "date": "2024-01-15T10:30:00Z"
    }
  }
}
```

## 💾 Куда сохраняются данные

После получения данных из Beliot API, показания сохраняются в **Google Sheets**:

1. **Лист:** "Показания счетчиков"
2. **Структура записи:**
   - `deviceId` - ID устройства (например, "11018")
   - `readingDate` - Дата снятия показания (Unix timestamp)
   - `readingValue` - Значение показания (например, 5401.37)
   - `unit` - Единица измерения (м³)
   - `readingType` - Тип сбора (`hourly` или `daily`)
   - `createdAt` - Дата создания записи в архиве
   - `source` - Источник (`api` - из Beliot API)

## 🔍 Что именно собирается

Система собирает **текущее значение счетчика** (например, накопительное значение в м³).

**Важно:** 
- Это **текущее** показание счетчика на момент запроса
- Это **не архивные данные** - система создает архив, собирая текущие показания периодически
- Для получения исторических данных нужно собирать показания регулярно (каждый час или каждый день)

## 📈 Схема потока данных

```
┌─────────────────┐
│  Beliot API     │
│  (Внешний API)  │
└────────┬────────┘
         │
         │ POST /api/device/attributes
         │ { device_id: "11018" }
         │
         ▼
┌─────────────────┐
│  Frontend App   │
│  (React)        │
│                 │
│  getBeliot      │
│  DeviceReadings │
└────────┬────────┘
         │
         │ Извлекает:
         │ - value (5401.37)
         │ - date (2024-01-15T10:30:00Z)
         │ - unit (м³)
         │
         ▼
┌─────────────────┐
│  Google Sheets  │
│  (Архив)        │
│                 │
│  Лист:          │
│  "Показания     │
│   счетчиков"    │
└─────────────────┘
```

## ⚙️ Настройка сбора

### Автоматический сбор

Система может собирать показания:
- **Почасово** (`hourly`) - каждый час
- **Ежедневно** (`daily`) - один раз в день (обычно в 00:00)

### Ручной сбор

Пользователь может запустить сбор вручную через интерфейс:
1. Выбрать устройство или все устройства
2. Выбрать тип сбора (почасовой/ежедневный)
3. Нажать "Собрать"

## 🔐 Требования

Для работы сбора данных необходимо:
1. **Доступ к Beliot API:**
   - API ключ настроен в `API_CONFIG.BELIOT_API_KEY`
   - Учетные данные для авторизации (email/password)
   - Токен доступа (получается автоматически)

2. **Доступ к Google Sheets:**
   - Google Apps Script развернут и доступен
   - Лист "Показания счетчиков" создан (создается автоматически)

## ⚠️ Важные замечания

1. **Текущие показания, не архив:**
   - Beliot API возвращает **текущее** значение счетчика
   - Для создания архива нужно собирать показания регулярно

2. **Ограничения Beliot API:**
   - API не предоставляет исторические данные напрямую
   - Поэтому мы создаем локальный архив в Google Sheets

3. **Частота сбора:**
   - Чем чаще собирать, тем более детальный архив
   - Рекомендуется: каждый час для точных данных

4. **Дубликаты:**
   - Система проверяет, есть ли уже показание за этот период
   - Если есть - пропускает (если не используется `force: true`)

