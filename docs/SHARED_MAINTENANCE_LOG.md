# Общий журнал обслуживания для нескольких единиц оборудования

## Описание

Теперь можно привязать несколько единиц оборудования (например, два фильтра) к одному журналу обслуживания. Это удобно, когда оборудование обслуживается вместе или нужно вести единый журнал для группы оборудования.

## Как использовать

### Вариант 1: Через редактирование оборудования (рекомендуется)

1. Откройте первое оборудование (например, Фильтр №1) и нажмите "Редактировать"
2. В форме редактирования найдите поле "Maintenance Sheet ID" (если оно есть) или добавьте его в форму
3. Создайте или скопируйте ID журнала обслуживания (Google Sheets ID)
4. Сохраните оборудование
5. Откройте второе оборудование (Фильтр №2) и нажмите "Редактировать"
6. Укажите тот же самый `maintenanceSheetId`, что и у первого фильтра
7. Сохраните оборудование

Теперь оба фильтра будут использовать один и тот же журнал обслуживания.

### Вариант 2: Через Google Sheets напрямую

1. Откройте таблицу "База данных оборудования" в Google Sheets
2. Найдите колонку "Maintenance Sheet ID" (или добавьте её, если её нет)
3. Для первого фильтра создайте журнал обслуживания (он создастся автоматически при первой записи) и скопируйте его ID
4. В колонке "Maintenance Sheet ID" для обоих фильтров укажите одинаковый ID
5. Сохраните изменения

## Технические детали

### Изменения во фронтенде

- `getMaintenanceLog()` теперь принимает опциональный параметр `maintenanceSheetId`
- `addMaintenanceEntry()` теперь поддерживает `maintenanceSheetId` для добавления записей в общий журнал
- Компоненты `MaintenanceLog` и `MaintenanceLogModal` передают `maintenanceSheetId` при его наличии

### Необходимые изменения в бэкенде (Google Apps Script)

Для полной поддержки общего журнала нужно обновить `backend/equipment-db/Code.gs`:

1. **В функции `getMaintenanceLog`:**
   - Добавить проверку параметра `maintenanceSheetId`
   - Если `maintenanceSheetId` указан, получать записи из общего журнала
   - Если не указан, использовать текущую логику (по `equipmentId`)

2. **В функции `_addMaintenanceEntry`:**
   - Добавить проверку параметра `maintenanceSheetId`
   - Если `maintenanceSheetId` указан, добавлять запись в общий журнал
   - При этом все равно указывать `equipmentId` в записи, чтобы знать, для какого оборудования была работа

### Пример обновления бэкенда

#### 1. Обновить обработку в `doGet`:

```javascript
case 'getMaintenanceLog':
  // Получить журнал обслуживания для оборудования
  const equipmentId = e.parameter.equipmentId;
  const maintenanceSheetId = e.parameter.maintenanceSheetId; // Новый параметр
  if (!equipmentId) {
    return createErrorResponse('ID оборудования не указан');
  }
  return createJsonResponse(getMaintenanceLog(equipmentId, maintenanceSheetId));
```

#### 2. Обновить функцию `getMaintenanceLog`:

```javascript
function getMaintenanceLog(equipmentId, maintenanceSheetId) {
  try {
    let sheet;
    
    // Если указан maintenanceSheetId, используем общий журнал
    if (maintenanceSheetId) {
      try {
        const spreadsheet = SpreadsheetApp.openById(maintenanceSheetId);
        sheet = spreadsheet.getSheetByName('Журнал обслуживания') || 
                spreadsheet.getSheets()[0];
        Logger.log('📄 Используется общий журнал обслуживания: ' + maintenanceSheetId);
      } catch (error) {
        Logger.log('⚠️ Ошибка при открытии общего журнала: ' + error);
        // Fallback на обычный журнал
        sheet = getMaintenanceLogSheet();
      }
    } else {
      // Обычный журнал в основной таблице
      sheet = getMaintenanceLogSheet();
    }
    
    const data = sheet.getDataRange().getValues();
    
    // Пропускаем заголовок (первая строка)
    const entries = [];
    for (let i = 1; i < data.length; i++) {
      const row = data[i];
      // Если это общий журнал, показываем все записи или фильтруем по equipmentId
      // Если обычный журнал, фильтруем только по equipmentId
      if (!maintenanceSheetId || row[0] === equipmentId) {
        entries.push({
          id: row[1],                    // ID записи
          equipmentId: row[0],           // ID оборудования
          date: row[2] ? formatDate(row[2]) : '',  // Дата обслуживания
          type: row[3] || '',            // Тип работы
          description: row[4] || '',     // Описание
          performedBy: row[5] || '',     // Выполнил
          status: row[6] || 'completed', // Статус
          createdAt: row[7] ? formatDate(row[7]) : '' // Дата создания
        });
      }
    }
    
    // Сортируем по дате обслуживания (новые сверху)
    entries.sort((a, b) => {
      const dateA = new Date(a.date);
      const dateB = new Date(b.date);
      return dateB - dateA;
    });
    
    return entries;
  } catch (error) {
    Logger.log('Ошибка при получении журнала обслуживания: ' + error);
    return [];
  }
}
```

#### 3. Обновить обработку в `doPost` для `addMaintenanceEntry`:

```javascript
case 'addMaintenanceEntry':
  // ...
  const equipmentId = String(data.equipmentId).trim();
  const maintenanceSheetId = data.maintenanceSheetId ? String(data.maintenanceSheetId).trim() : null;
  // ...
  return createJsonResponse(_addMaintenanceEntry(equipmentId, entryData, maintenanceSheetId));
```

#### 4. Обновить функцию `_addMaintenanceEntry`:

```javascript
function _addMaintenanceEntry(equipmentId, entry, maintenanceSheetId) {
  // ...
  
  let sheet;
  
  // Если указан maintenanceSheetId, используем общий журнал
  if (maintenanceSheetId) {
    try {
      const spreadsheet = SpreadsheetApp.openById(maintenanceSheetId);
      sheet = spreadsheet.getSheetByName('Журнал обслуживания') || 
              spreadsheet.getSheets()[0];
      Logger.log('📄 Добавление записи в общий журнал: ' + maintenanceSheetId);
    } catch (error) {
      Logger.log('⚠️ Ошибка при открытии общего журнала: ' + error);
      // Fallback на обычный журнал
      sheet = getMaintenanceLogSheet();
    }
  } else {
    // Обычный журнал в основной таблице
    sheet = getMaintenanceLogSheet();
  }
  
  // Остальная логика добавления записи...
  // ...
}
```

## Важные замечания

1. **ID журнала** - это ID Google Sheets файла (можно найти в URL: `https://docs.google.com/spreadsheets/d/{ID}/edit`)

2. **При добавлении записи** в общий журнал, запись все равно будет содержать `equipmentId`, чтобы знать, для какого оборудования была выполнена работа

3. **При просмотре журнала** можно показывать все записи из общего журнала или фильтровать по `equipmentId` конкретного оборудования

4. **Рекомендуется** создать отдельный файл журнала для группы оборудования и указать его ID в `maintenanceSheetId` для всех единиц этой группы

## Пример использования

Допустим, у вас есть два фильтра: "Фильтр №1" и "Фильтр №2", которые обслуживаются вместе.

1. Создайте новый Google Sheets файл "Журнал обслуживания - Фильтры 1-2"
2. Скопируйте ID файла из URL
3. В базе данных оборудования укажите этот ID в поле "Maintenance Sheet ID" для обоих фильтров
4. Теперь при открытии журнала обслуживания любого из фильтров будет отображаться общий журнал
5. При добавлении записи она будет добавляться в общий журнал с указанием `equipmentId` соответствующего фильтра

