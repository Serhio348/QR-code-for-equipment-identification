# Стратегия синхронизации данных счетчиков Beliot

## 📋 Текущая архитектура

### Как работает сейчас:
1. **Данные из API** - запрашиваются каждый раз при загрузке приложения
2. **Overrides (пользовательские изменения)** - хранятся в Google Sheets (лист "Счётчики Beliot")
   - `name` - пользовательское название
   - `address` - пользовательский адрес
   - `serialNumber` - серийный номер (введенный вручную)
   - `group` - группа устройства
3. **Приоритет данных при отображении:**
   - Приоритет 1: localStorage (локальные изменения, еще не синхронизированные)
   - Приоритет 2: Google Sheets (синхронизированные изменения)
   - Приоритет 3: API (исходные данные из Beliot API)

## 🎯 Рекомендуемое решение

### Вариант 1: Текущий подход (рекомендуется)
**Не сохранять полные данные счетчиков в таблицу**

**Преимущества:**
- ✅ Всегда актуальные данные из API
- ✅ Нет конфликтов синхронизации
- ✅ Простая архитектура
- ✅ Меньше места в Google Sheets

**Как работает:**
- Данные запрашиваются из API при каждой загрузке
- Пользовательские изменения (overrides) применяются поверх данных API
- Если пользователь изменил название в таблице → используется название из таблицы
- Если пользователь НЕ изменил название → используется название из API

**Пример:**
```
API вернул: name = "MTK-40N тДЦ13001660"
Таблица содержит: name = "Счетчик ХВО-1" (override)
Отображается: "Счетчик ХВО-1" (приоритет у таблицы)
```

### Вариант 2: Полное сохранение с синхронизацией
**Сохранять полные данные + синхронизация с API**

**Структура листа "Счётчики Beliot":**
| Колонка | Название | Описание |
|---------|----------|----------|
| A | deviceId | ID устройства (уникальный ключ) |
| B | name | Название (override или из API) |
| C | address | Адрес (override или из API) |
| D | serialNumber | Серийный номер (override или из API) |
| E | group | Группа устройства |
| F | model | Модель счетчика (из API) |
| G | manufacturer | Производитель (из API) |
| H | lastSync | Дата последней синхронизации с API |
| I | lastModified | Дата последнего изменения пользователем |
| J | modifiedBy | Email пользователя |
| K | isOverride | Флаг: является ли поле пользовательским изменением |

**Логика синхронизации:**
1. При загрузке приложения:
   - Запрашиваем данные из API
   - Загружаем данные из Google Sheets
   - Для каждого поля:
     - Если `isOverride = true` → используем значение из таблицы
     - Если `isOverride = false` → обновляем значение из API
2. При сохранении пользовательского изменения:
   - Устанавливаем `isOverride = true` для измененного поля
   - Обновляем `lastModified` и `modifiedBy`

**Преимущества:**
- ✅ История изменений
- ✅ Работает офлайн (после первой загрузки)
- ✅ Можно видеть, что изменилось в API

**Недостатки:**
- ❌ Сложнее реализация
- ❌ Нужна синхронизация
- ❌ Больше места в Google Sheets

## 🔄 Рекомендация

**Использовать Вариант 1 (текущий подход)** по следующим причинам:

1. **Простота:** Не нужно синхронизировать полные данные
2. **Актуальность:** Всегда получаем свежие данные из API
3. **Гибкость:** Пользователь может изменить только нужные поля
4. **Производительность:** Меньше данных в Google Sheets

### Если нужна история изменений:
Создать отдельный лист "История изменений счетчиков" для логирования:
- Когда изменилось значение в API
- Когда пользователь изменил override
- История показаний (уже есть план в `DEVICE_READINGS_SAVE_PLAN.md`)

## 📝 Пример работы текущей системы

### Сценарий 1: Пользователь изменил название счетчика
```
1. API вернул: name = "MTK-40N тДЦ13001660"
2. Пользователь изменил в приложении на: "Счетчик ХВО-1"
3. Сохраняется в Google Sheets: { deviceId: "10596", name: "Счетчик ХВО-1", isOverride: true }
4. При следующей загрузке:
   - API вернул: name = "MTK-40N тДЦ13001660" (новое значение)
   - Таблица содержит: name = "Счетчик ХВО-1" (override)
   - Отображается: "Счетчик ХВО-1" (приоритет у override)
```

### Сценарий 2: Пользователь НЕ изменил название
```
1. API вернул: name = "MTK-40N тДЦ13001660"
2. Пользователь НЕ изменял название
3. В таблице нет override для name
4. При следующей загрузке:
   - API вернул: name = "MTK-40N тДЦ13001660" (новое значение)
   - Таблица не содержит override для name
   - Отображается: "MTK-40N тДЦ13001660" (из API)
```

### Сценарий 3: Изменилась марка счетчика в API
```
1. API вернул: model = "MTK-40N"
2. Пользователь НЕ изменял модель
3. В таблице нет override для model
4. При следующей загрузке:
   - API вернул: model = "MTK-50N" (новое значение)
   - Таблица не содержит override для model
   - Отображается: "MTK-50N" (автоматически обновилось из API)
```

## 🚀 Если нужно сохранять полные данные

Если все же нужно сохранять полные данные счетчиков, рекомендуется:

1. **Создать отдельный лист "Архив счетчиков Beliot"** для полных данных
2. **Оставить лист "Счётчики Beliot"** только для overrides
3. **Реализовать синхронизацию:**
   - При загрузке: обновлять архив из API
   - При отображении: использовать API + overrides
   - Архив используется только для истории/аналитики

