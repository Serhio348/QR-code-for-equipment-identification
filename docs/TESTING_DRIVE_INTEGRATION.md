# Тестирование интеграции с Google Drive

Этот документ описывает процесс тестирования интеграции с Google Drive для проверки прав администратора.

## Подготовка

### 1. Включите Drive API (рекомендуется)

1. Откройте проект в Google Apps Script
2. Перейдите в меню: **Ресурсы → Расширенные сервисы Google**
3. Найдите **"Drive API"** в списке
4. Включите его (переключатель справа)
5. Нажмите **"ОК"**

**Примечание:** Если Drive API не включен, система будет использовать fallback через `DriveApp.getOwners()`, что также работает, но менее надежно.

### 2. Предоставьте разрешения

При первом запуске тестовых функций Google запросит разрешения:
1. Нажмите **"Разрешить"**
2. Выберите ваш аккаунт Google
3. Нажмите **"Продолжить"**
4. Разрешите доступ к **Google Drive**

## Тестовые функции

Все тестовые функции находятся в файле `AdminSetup.gs` и доступны через меню **Выполнить** в Google Apps Script редакторе.

### 1. `testGetDriveFolderOwners`

**Назначение:** Проверяет получение владельцев корневой папки Google Drive.

**Как запустить:**
1. Откройте Google Apps Script редактор
2. В меню выберите: **Выполнить → testGetDriveFolderOwners**
3. Просмотрите результаты в логах (Вид → Логи)

**Что проверяет:**
- Получение ID корневой папки
- Получение списка владельцев папки
- Вывод email адресов всех владельцев

**Ожидаемый результат:**
```
✅ Результат:
   Найдено владельцев: 1 (или больше)
   📧 Email адреса владельцев:
      1. your-email@gmail.com
```

### 2. `testVerifyAdminForCurrentUser`

**Назначение:** Проверяет права администратора для текущего пользователя (того, кто запускает скрипт).

**Как запустить:**
1. Откройте Google Apps Script редактор
2. В меню выберите: **Выполнить → testVerifyAdminForCurrentUser**
3. Просмотрите результаты в логах

**Что проверяет:**
- Определение email текущего пользователя
- Проверка прав администратора
- Сравнение с полным списком администраторов

**Ожидаемый результат:**
```
✅ Результат:
   Email: your-email@gmail.com
   Роль: admin (или user)
   Является администратором: ✅ ДА (или ❌ НЕТ)
```

### 3. `testVerifyAdminForEmail`

**Назначение:** Проверяет права администратора для конкретного email адреса.

**Как использовать:**
1. Откройте файл `AdminSetup.gs`
2. Найдите функцию `testVerifyAdminForEmail`
3. Измените строку `const testEmail = 'user@example.com';` на реальный email
4. Сохраните файл
5. Запустите: **Выполнить → testVerifyAdminForEmail**

**Что проверяет:**
- Проверка прав для указанного email
- Поиск в списке всех администраторов
- Определение источника прав (Google Drive или резервный список)

### 4. `testFullDriveIntegration`

**Назначение:** Выполняет полный набор тестов интеграции с Google Drive.

**Как запустить:**
1. Откройте Google Apps Script редактор
2. В меню выберите: **Выполнить → testFullDriveIntegration**
3. Просмотрите результаты в логах

**Что проверяет:**
- Получение ID папки
- Получение владельцев папки
- Получение всех администраторов
- Проверка прав для текущего пользователя
- Вывод итоговой статистики

**Ожидаемый результат:**
```
📊 ИТОГИ ТЕСТИРОВАНИЯ:
   - ID папки: ✅
   - Владельцы папки: 1
   - Всего администраторов: 1
   - Интеграция с Google Drive: ✅ Работает
```

## Проверка через веб-интерфейс

После тестирования в Google Apps Script, проверьте работу через веб-интерфейс:

### 1. Регистрация нового пользователя

1. Откройте веб-приложение
2. Зарегистрируйте нового пользователя с email, который является владельцем корневой папки Google Drive
3. После регистрации пользователь должен автоматически получить роль 'admin'

### 2. Вход существующего пользователя

1. Войдите в систему с email владельца Google Drive папки
2. Проверьте, что пользователь видит полный интерфейс (список оборудования, кнопки редактирования)
3. Если пользователь не является администратором, он должен видеть только журнал обслуживания и документацию

### 3. Проверка списка администраторов

1. Войдите как администратор
2. Откройте лист "Администраторы" в Google Sheets
3. Проверьте, что владельцы Google Drive папки автоматически появляются в списке (через функцию `getAllAdmins()`)

## Устранение проблем

### Проблема: Владельцы папки не найдены

**Возможные причины:**
1. Drive API не включен
2. Не предоставлены разрешения на доступ к Google Drive
3. Папка недоступна

**Решение:**
1. Включите Drive API: **Ресурсы → Расширенные сервисы Google → Drive API**
2. Запустите `requestDrivePermissions()` для запроса разрешений
3. Проверьте, что папка существует и доступна

### Проблема: Пользователь не получает роль администратора

**Возможные причины:**
1. Email пользователя не совпадает с email владельца папки (регистр, пробелы)
2. Пользователь не зарегистрирован в системе
3. Функция `verifyAdminAccess` не вызывается при входе

**Решение:**
1. Проверьте точное совпадение email адресов (система нормализует их к lowercase)
2. Убедитесь, что пользователь зарегистрирован
3. Проверьте логи в Google Apps Script при входе пользователя

### Проблема: Drive API недоступен

**Решение:**
Система автоматически использует fallback через `DriveApp.getOwners()`. Это нормально и должно работать. Проверьте логи, чтобы убедиться, что fallback используется.

## Дополнительные функции для отладки

### `checkAdminStatus(email)`

Проверяет статус администратора для конкретного email:
```javascript
checkAdminStatus('user@example.com');
```

### `listAllAdmins()`

Выводит список всех администраторов (из Google Drive и резервного списка):
```javascript
listAllAdmins();
```

## Логирование

Все функции используют `Logger.log()` для вывода информации. Чтобы просмотреть логи:

1. В Google Apps Script редакторе: **Вид → Логи**
2. Или откройте: **Выполнить → Просмотр → Логи выполнения**

Логи содержат эмодзи для удобства:
- ✅ - Успешная операция
- ❌ - Ошибка
- ⚠️ - Предупреждение
- 🔍 - Проверка/поиск
- 📁 - Операции с папками
- 👤 - Информация о пользователе
- 👑 - Информация об администраторе

