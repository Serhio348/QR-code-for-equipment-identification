# План реализации журнала обслуживания через Google Drive

## Цель
Создать журнал обслуживания оборудования, доступный по ссылке из Google Drive, который будет работать в режиме онлайн и позволит вести записи обслуживания.

## Варианты реализации

### Вариант 1: Google Apps Script (Рекомендуемый) ⭐

**Преимущества:**
- Бесплатный хостинг
- Интеграция с Google Drive
- Хранение данных в Google Sheets
- Не требует отдельного сервера
- Легко делиться ссылкой

**Шаги реализации:**

1. **Создание Google Sheets для хранения данных**
   - Создать новый Google Sheets документ
   - Настроить структуру таблицы:
     - Дата обслуживания
     - Номер фильтра (1 или 2)
     - Тип обслуживания
     - Описание
     - Выполнил (ФИО)
     - Время создания записи

2. **Создание веб-приложения на Google Apps Script**
   - Открыть Google Apps Script (script.google.com)
   - Создать новый проект
   - Написать код для:
     - Отображения формы добавления записи
     - Отображения истории записей
     - Сохранения данных в Google Sheets
     - Фильтрации по номеру фильтра
     - Экспорта данных

3. **Развертывание как веб-приложение**
   - Опубликовать скрипт как веб-приложение
   - Настроить права доступа (все пользователи с ссылкой)
   - Получить URL приложения

4. **Добавление ссылки в Google Drive**
   - Создать документ Google Docs или файл с описанием
   - Добавить ссылку на веб-приложение
   - Разместить в папке на Google Drive

**Технические детали:**
- Использовать HTML Service для UI
- Использовать Google Sheets API для работы с данными
- Реализовать REST API для добавления/получения записей

---

### Вариант 2: Отдельный хостинг (Netlify/Vercel)

**Преимущества:**
- Полный контроль над кодом
- Использование React/TypeScript
- Современный стек технологий

**Шаги реализации:**

1. **Создание отдельного приложения журнала**
   - Вынести компонент MaintenanceLog в отдельный проект
   - Настроить бэкенд (Firebase, Supabase, или другой)
   - Реализовать аутентификацию (опционально)

2. **Развертывание на хостинге**
   - Netlify/Vercel для фронтенда
   - Настроить домен или использовать поддомен
   - Получить URL приложения

3. **Добавление ссылки в Google Drive**
   - Создать документ со ссылкой
   - Разместить в папке Google Drive

**Недостатки:**
- Требует настройки бэкенда
- Может потребоваться оплата за хостинг данных

---

### Вариант 3: Google Forms + Google Sheets

**Преимущества:**
- Простейшая реализация
- Не требует программирования
- Автоматическое сохранение в таблицу

**Шаги реализации:**

1. **Создание Google Form**
   - Создать форму с полями:
     - Дата обслуживания
     - Номер фильтра
     - Тип обслуживания
     - Описание
     - Выполнил
   - Настроить автоматическое сохранение в Google Sheets

2. **Создание Google Sheets для просмотра**
   - Настроить таблицу для отображения записей
   - Добавить фильтры и сортировку
   - Настроить права доступа

3. **Добавление ссылок в Google Drive**
   - Ссылка на форму (для добавления записей)
   - Ссылка на таблицу (для просмотра истории)

**Недостатки:**
- Ограниченный функционал
- Менее удобный интерфейс
- Нет возможности редактирования записей через форму

---

## Рекомендуемый план действий (Вариант 1)

### Этап 1: Подготовка (30 минут)
1. Создать Google Sheets документ "Журнал обслуживания фильтров"
2. Настроить структуру таблицы
3. Создать папку в Google Drive для журнала

### Этап 2: Разработка (2-3 часа)
1. Создать Google Apps Script проект
2. Реализовать HTML интерфейс:
   - Форма добавления записи
   - Список записей с фильтрацией
   - Поиск по дате/типу обслуживания
3. Реализовать логику работы с Google Sheets:
   - Функция добавления записи
   - Функция получения записей
   - Функция фильтрации по номеру фильтра
4. Добавить валидацию данных

### Этап 3: Тестирование (30 минут)
1. Протестировать добавление записей
2. Проверить отображение истории
3. Проверить фильтрацию
4. Протестировать на разных устройствах

### Этап 4: Развертывание (15 минут)
1. Опубликовать как веб-приложение
2. Настроить права доступа
3. Получить URL приложения
4. Создать документ в Google Drive со ссылкой
5. Разместить в папке с документацией

### Этап 5: Интеграция (15 минут)
1. Добавить ссылку на журнал в документ на Google Drive
2. Обновить описание в папке
3. Протестировать доступ по QR-коду

---

## Структура данных в Google Sheets

| Дата | Номер фильтра | Тип обслуживания | Описание | Выполнил | Время создания |
|------|---------------|------------------|----------|----------|----------------|
| 2024-01-15 | 1 | Промывка | Промывка фильтра обратным потоком | Иванов И.И. | 2024-01-15 10:30 |
| 2024-01-20 | 2 | Замена засыпки | Замена засыпки Nevtraco | Петров П.П. | 2024-01-20 14:00 |

---

## Функциональные требования

### Обязательные функции:
- ✅ Добавление новой записи обслуживания
- ✅ Просмотр истории обслуживания
- ✅ Фильтрация по номеру фильтра (1 или 2)
- ✅ Сортировка по дате
- ✅ Отображение последней даты обслуживания

### Дополнительные функции:
- 🔍 Поиск по типу обслуживания
- 📊 Статистика обслуживания
- 📄 Экспорт в PDF/Excel
- 🔔 Напоминания о следующем обслуживании
- 📱 Адаптивный дизайн для мобильных устройств

---

## Пример структуры кода Google Apps Script

```javascript
// Code.gs
function doGet() {
  return HtmlService.createTemplateFromFile('Index')
    .evaluate()
    .setTitle('Журнал обслуживания фильтров')
    .setXFrameOptionsMode(HtmlService.XFrameOptionsMode.ALLOWALL);
}

function getMaintenanceRecords(filterNumber) {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  const data = sheet.getDataRange().getValues();
  // Фильтрация и возврат данных
  return filteredData;
}

function addMaintenanceRecord(record) {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  sheet.appendRow([
    record.date,
    record.filterNumber,
    record.type,
    record.description,
    record.performedBy,
    new Date()
  ]);
}
```

---

## Безопасность

1. **Права доступа:**
   - Настроить доступ "Все, у кого есть ссылка"
   - Ограничить редактирование (только добавление записей)
   - Администратор может редактировать через Google Sheets

2. **Валидация данных:**
   - Проверка формата даты
   - Проверка обязательных полей
   - Ограничение длины текста

3. **Резервное копирование:**
   - Google Sheets автоматически сохраняет историю изменений
   - Можно настроить автоматический экспорт

---

## Альтернативный вариант: Использование существующего компонента

Если нужно использовать существующий React компонент MaintenanceLog:

1. **Создать отдельный проект для журнала**
   - Вынести MaintenanceLog в отдельный репозиторий
   - Настроить Firebase/Supabase для хранения данных
   - Развернуть на Netlify/Vercel

2. **Интеграция с Google Drive**
   - Создать HTML страницу в Google Drive
   - Встроить iframe с ссылкой на приложение
   - Или просто добавить прямую ссылку

---

## Временные затраты

- **Вариант 1 (Google Apps Script):** 3-4 часа
- **Вариант 2 (Отдельный хостинг):** 4-6 часов
- **Вариант 3 (Google Forms):** 30 минут

## Рекомендация

**Использовать Вариант 1 (Google Apps Script)** - оптимальное соотношение функциональности, простоты реализации и стоимости (бесплатно).

