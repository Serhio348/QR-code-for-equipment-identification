# Решение проблемы CORS для POST запросов

## Проблема
POST запросы блокируются CORS ошибкой: "Response to preflight request doesn't pass access control check"

## Решение

### Шаг 1: Проверьте настройки веб-приложения

1. Откройте Google Apps Script проект
2. **Развернуть → Управление развертываниями**
3. Нажмите на **карандаш** ✏️ рядом с существующим развертыванием
4. **КРИТИЧЕСКИ ВАЖНО:** Убедитесь, что **"У кого есть доступ"** установлено в **"Все"**
   - ❌ НЕ "Только я"
   - ❌ НЕ "Пользователи организации"
   - ✅ ДОЛЖНО БЫТЬ "Все"
5. Нажмите **"Развернуть"** для обновления

### Шаг 2: Убедитесь, что код обновлен

1. В Google Apps Script убедитесь, что код из `backend/equipment-db/Code.gs` скопирован
2. Функция `doOptions` должна быть в коде
3. Сохраните проект (Ctrl+S)

### Шаг 3: Создайте НОВОЕ развертывание (если проблема сохраняется)

Если обновление существующего развертывания не помогло:

1. **Развернуть → Управление развертываниями**
2. Удалите старое развертывание (корзина 🗑️)
3. **Развернуть → Новое развертывание**
4. Выберите **Веб-приложение**
5. Настройки:
   - **Выполнять от имени:** Меня
   - **У кого есть доступ:** **Все** ⚠️
6. Нажмите **Развернуть**
7. Скопируйте новый URL и обновите в `src/config/api.ts`

### Шаг 4: Проверьте в браузере

Откройте URL веб-приложения напрямую в браузере:
```
https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec?action=getAll
```

Должен вернуться JSON ответ (даже если пустой массив).

## Альтернативное решение (если CORS все еще не работает)

Если проблема сохраняется, можно использовать обходной путь:

1. После POST запроса сразу делать GET запрос для проверки результата
2. Или использовать Google Apps Script API напрямую через OAuth (более сложно)

## Проверка

После настройки попробуйте снова:
- ✅ GET запросы должны работать
- ✅ POST запросы должны работать (после правильной настройки)

Если проблема сохраняется, проверьте консоль браузера (F12) для детальной информации об ошибке.

