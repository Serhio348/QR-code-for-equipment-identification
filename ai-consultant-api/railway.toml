[build]
builder = "DOCKERFILE"
dockerfilePath = "ai-consultant-api/Dockerfile"

[deploy]
# Без startCommand — используется CMD из Dockerfile: node dist/index.js
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
